# Decisions made without a human

Entries added by unattended runs under docs/07_UNATTENDED.md rule 2.3.
Format: `- [M<n>] <date> <decision>: <why>; <how to reverse>`.
Cadu reviews these; a decision he rejects is reversed in a follow-up task and
the vision doc is amended if the question deserved a real answer.
- [M1] 2026-09-08 `rustible init` must generate `[profile.dist]` and `.cargo/config.toml` (rust-lld per musl target) in every workspace: a detached workspace inherits neither from the repo; found when the orchestrator's dist build failed against `examples/workspace`. Reverse: none needed, this is a gap in the M4 brief, not a design change.
- [M1] 2026-09-08 `#[rustible::vars]` rejects maps and tuples syntactically; nested structs cannot be told from enums in a proc macro, so the orchestrator's vars validation (M2/M3) must reject schema properties of `type: object`. Reverse: add a `#[vars]`-side check if a reliable syntactic rule appears.
- [M1] 2026-09-08 `--var k=v` parses `v` as JSON when it parses, else as a string (numbers, bools, lists work without quoting). Reverse: make it string-only and require typed syntax.
- [M1] 2026-09-08 Vars deserialization does NOT use `deny_unknown_fields`: the host var bag is shared by all playbooks targeting the host (vision 10.3), so undeclared keys are legitimate; the runtime warns per undeclared key with a did-you-mean instead. Reverse: add `deny_unknown_fields` in the `#[vars]` expansion.
- [M1] 2026-09-08 Alternating `rustible playbook run` between two playbooks re-links the bin each time (one `selected` OUT_DIR, `rerun-if-env-changed`). Observation for M3: weigh a per-playbook artifact cache keyed by (playbook, triple, source hash) against duplicated dependency artifacts. Reverse: n/a, nothing built.
- [M1] 2026-09-08 `rust-version` raised from 1.85 to 1.88 because the code uses let-chains (stable in 1.88, edition 2024). Reverse: nest the `if let`s.
- [M1] 2026-09-08 Vars flatness is enforced on the generated JSON Schema (`vars::flatness_violations`, resolving `$ref`s) at `--describe` and at `Start`; the macro's syntactic map/tuple rejection is only an early hint. Supersedes the earlier M1 note that deferred this to M2/M3.
- [M4] 2026-09-08 `rustible init` treats a directory holding only `.git` as empty: `git init` before `rustible init` is the common order and vision 10.4 already expects a git repository. Reverse: drop the `.git` exception in `is_empty_dir`.
- [M4] 2026-09-08 `rustible init --name <name>` overrides the package name derived from the directory, and names Cargo refuses (Rust keywords, `test`, `core`, `std`, ...) or that would shadow a dependency (`rustible`, `rustible-std`, ...) are rejected with a message pointing at `--name`: a directory called `type` or `rustible` would otherwise produce a workspace that cannot build. Reverse: remove the flag and the reserved list.
- [M4] 2026-09-08 `.gitignore` lines are `/target` and `/.rustible` (root-anchored, as in `examples/workspace`), and an existing `.gitignore` is appended to whether or not the directory is a git repository (the brief's "inside a git repository" condition only matters when the file exists, and then appending is right either way). Reverse: write `target/` and `.rustible/` and check for `.git` before appending.
- [M4] 2026-09-08 `--path-deps <checkout>` is written into the manifest as given (a relative path is relative to the new workspace, like Cargo's own `path =`), after checking that `<checkout>/crates/{rustible,rustible-std,rustible-build}/Cargo.toml` exist. Reverse: canonicalize to absolute.
- [M4] 2026-09-08 The regeneration test compares `Cargo.toml`, `build.rs`, `src/main.rs` and `.cargo/config.toml` of `examples/workspace` byte for byte, including the version in the shim headers; a version bump therefore requires `rustible init --refresh examples/workspace` (one command, and the test's failure message says so). `src/lib.rs`, `hosts.kdl` and `playbooks/` carry example content and are not compared. Reverse: normalize the version in the test.
- [M4] 2026-09-08 `rustible playbook create` adds `.rs` when the path lacks it, refuses to overwrite an existing file, and warns (but still writes) when the path is outside the workspace's `playbooks/` or outside any workspace, because the build script would not discover it. Reverse: make these errors.
- [M4] 2026-09-08 `--force` keeps existing user-owned files and rewrites only the two shims (review found it overwrote hosts.kdl and src/lib.rs). Reverse: make --force overwrite everything again.
- [M4] 2026-09-08 PROPOSED AMENDMENT for Cadu: `rustible init` refuses any non-empty directory (vision 3); a fresh GitHub clone with README, LICENSE and .gitignore is refused and needs --force. cargo-init-style conflict-based refusal (only when a file init would write already exists) would be friendlier. Not changed; vision doc wins.
- [M4] 2026-09-08 Post-init hint prints `cargo run -- hello` with `rustible playbook run` marked "once available", because M3 lands after M4. Reverse: switch the hint when M3 merges (M3 brief should do this).
- [M6-harness] 2026-09-08 Integration tests are opt-in with `RUSTIBLE_INTEGRATION=1` and skip (pass, printing why) without it or without a working `docker`; the brief's "runs in CI when docker is available" becomes "CI sets the variable where docker exists". A plain `cargo test --workspace` never pulls images or builds for musl. Reverse: drop the variable check in `testing::skip_reason` and gate on `docker info` alone.
- [M6-harness] 2026-09-08 The test binary is rebuilt for `<arch>-unknown-linux-musl` in release from inside the running test (`cargo test --no-run --release --target ... --test <file>`, once per process) rather than by an outer wrapper or build script; vision 8.3 wants a static binary and this keeps `cargo test` the only entry point. Consequence: test file names must be valid crate names (underscores). Reverse: a `cargo rustible-test` wrapper that builds first and passes the path in.
- [M6-harness] 2026-09-08 `rustible-std` takes `rustible` as a dev-dependency (a dev-only cycle, the same shape `rustible-macros` already uses) so `tests/` can write `#[rustible::integration_test]` as an op author would. Reverse: re-export the attribute from `rustible-sdk` and drop the dev-dependency.
- [M6-harness] 2026-09-08 The brief's `SystemdImage` variant is `testing::Image::Systemd`, written as `systemd_images = ["..."]` next to `images = [...]` in the attribute (string literals only, so both lists share one syntax). Systemd containers run `--privileged --cgroupns=host` with the host cgroup mounted; the documented images are the third-party `jrei/systemd-debian:12` and `jrei/systemd-ubuntu:24.04` because stock images ship no systemd and installing it per run costs tens of seconds. Reverse: an `images = [systemd("...")]` syntax, or a `Dockerfile` under `crates/rustible-sdk/` that builds our own systemd images.
- [M6-harness] 2026-09-08 In-container detection requires `RUSTIBLE_INTEGRATION_INSIDE` plus `/t`; explicit `RUSTIBLE_INTEGRATION=1` with no docker or an empty image filter is a failure, not a skip. Container bodies run under `timeout 600`; systemd containers have a Drop guard.
- [M6-harness] 2026-09-08 DEFERRED to M7: replace the harness's own Report/StepReport wire format with a replay of `event::Event` via JsonLines (design-quality only).
- [M6-harness] 2026-09-08 Systemd images keep `--cgroupns=host` and the host cgroup bind mount because the jrei images (VOLUME /sys/fs/cgroup) do not boot otherwise; a purpose-built image would allow plain `--privileged`. Reverse: build `rustible/systemd-debian` from six lines and drop both flags.
- [M2] 2026-09-08 Sibling-group conflicts are checked for parameters as well as vars: vision 10.3 decides the rule for vars and 10.2.1 says "nearest group outward" for parameters without saying what a tie means; the same rule keeps `ssh_user` from silently depending on file order. Reverse: skip `ConflictKind::Param` in `check_conflicts`.
- [M2] 2026-09-08 Group distance is the shortest path from the host: the group a host is nested in and every group that lists it in `members` are distance 1, a group listing a distance-d group is d+1, and a group reachable by two paths takes the shorter one. Reverse: change `group_levels`.
- [M2] 2026-09-08 Conflicts are detected at load (every host is resolved in `check_conflicts`, so `inventory check` reports them with the host's line) and `resolve()` re-checks; after a clean load it can only fail on an unknown host. Reverse: drop `check_conflicts` and let `resolve` report.
- [M2] 2026-09-08 `ssh_args` has two spellings because a KDL property holds one value: `ssh_args="-4"` (one string) or a child node `ssh_args "-o" "X"` (the list), and both count as the same parameter for "set twice". Reverse: drop the property form.
- [M2] 2026-09-08 `#null` is rejected in `vars` ("leave a var out instead"), integers must fit i64, lists are positional arguments and may mix scalar types (the schema check reports the type, not the parser). Reverse: map `#null` to an absent key.
- [M2] 2026-09-08 A `group` node lexically inside a `group` is an error pointing at `members` (vision 10.1: groups contain groups through `members`); a host is nested in at most one group by placement (10.2.3) and referenced elsewhere by name. Reverse: allow lexical nesting as sugar for `members`.
- [M2] 2026-09-08 Undeclared vars are warnings, and a schema property of `type: object` is an error, as M1 recorded; `inventory check` validates the file only until M3 wires `--describe`, and the schema-facing API (`validate`, `format_vars_report`) is exercised by tests against a fixture of the M1 `--describe` shape. Reverse: n/a.
- [M2] 2026-09-08 The spike orchestrator's flat flags moved under `rustible run` unchanged (vision 3 is noun-first: `inventory show`, `playbook run`); M3 deletes `run`. Reverse: n/a, temporary.
- [M2] 2026-09-08 `inventory show` prints lists as `[a, b]` and other scalars in KDL 2.0 spelling (`#true`, quoted strings). `[..]` is not KDL (lists are positional arguments there) but reads unambiguously in a table. Reverse: print lists space-separated.
- [M2] 2026-09-08 A single positional var value is a scalar; where the playbook schema wants a list it is coerced to a one-element list on both sides (`vars::coerce_scalars_to_lists`); a var with no values is an empty list. Reverse: add an explicit list syntax and drop the coercion.
- [M2] 2026-09-08 Non-finite floats are refused at parse; integers above i64::MAX are carried as `Scalar::UInt`.
- [M2] 2026-09-08 The built-in `ssh_user` is USER, then LOGNAME, then the uid's /etc/passwd entry, else left to ssh's default (never a `root` guess).
- [M2] 2026-09-08 FOR M3: the orchestrator pre-check validator is a JSON Schema subset; give the `--describe` host build a `--check-vars` mode so serde is the single validator on both sides. Not done in M2.
- [M6] 2026-09-08 `ssh::authorized_keys::Present` (user form) refuses when the user's home directory does not exist instead of creating it root-owned on the way to `~/.ssh` (vision 6.7); `~/.ssh` itself is created because the brief and Ansible's `manage_dir` default ask for it. Reverse: drop the home check in `check_parent`.
- [M6] 2026-09-08 `ssh::authorized_keys`: a requested line that does not parse as a public key fails the step at `check` instead of being appended verbatim, so a playbook typo cannot land in `authorized_keys`. Reverse: append unparseable lines untouched.
- [M6] 2026-09-08 `ssh::authorized_keys`: key identity is `(key_type, key)`; options and comment are ignored for matching and an existing line is never rewritten to match the requested options or comment (Ansible ignores the comment but rewrites options). Reverse: compare options in `same_key` or rewrite the raw line on mismatch.
- [M6-ak] 2026-09-08 `ssh::authorized_keys` requires `~/.ssh` to exist (vision 6.7, decided); the earlier branch decision to create it was reversed on review. Reverse: re-add the create branch and record an amendment.
- [M6-ak] 2026-09-08 A symlinked `authorized_keys` file is refused (atomic rewrite would replace the link); `in_file(real path)` is the workaround. Reverse: resolve the link target and write there.
- [M6-ak] 2026-09-08 User lookup reads `/etc/passwd` only; NSS-only accounts (LDAP, SSSD, systemd-homed) are unsupported by the user-name form, `for_account` covers them. Reverse: route through `getent passwd` via `sys.cmd`.
- [M6-ak] 2026-09-08 SDK gained `stat_follow` (follows symlinks) next to the lstat-based `stat`. Additive.
- [M6-ug] 2026-09-08 `user::Present` and `group::Present` predict their output only when it is honest (vision 12): an existing account or group is always predicted; a new one is predicted only when the uid (gid) is given, and for a user the primary gid must be given explicitly (the earlier private-group `gid == uid` guess was dropped on review: login.defs ranges are not modelled). Otherwise `Plan::change` without a prediction, so a check-mode run that chains from the step stops with the vision's clear message. Reverse: predict with a sentinel and document it.
- [M6-ug] 2026-09-08 `apply` in `user::Present` and `group::Present` executes the `Diff::Attrs` that `check` produced (create when an `exists` change is present, else `Delta::from_changes` for `usermod`) instead of inspecting the system again, then re-reads `/etc/passwd` and `/etc/group` for the real output. Vision 6.2 ("`check` does all the thinking; `apply` executes that diff") and it is what makes the Fake tests able to plant the tool's effect. Reverse: re-run the inspection in `apply`.
- [M6-ug] 2026-09-08 `Present::append` defaults to `true` (Ansible's default is `false`): adding a user to `docker` must not silently drop them from `sudo`; `.append(false)` gives the exact-set semantics. Reverse: flip the default.
- [M6-ug] 2026-09-08 On Alpine (BusyBox) an existing account's uid, gid, home, shell or comment cannot be changed (no `usermod`) and an existing group's gid cannot be changed (no `groupmod`); both fail at `check` naming the `shadow` package. Group membership changes work through `addgroup`/`delgroup`. Reverse: shell out to `usermod` when present.
- [M6-ug] 2026-09-08 `Present::gid` takes `impl Into<GroupId>` (a `u32`, a `&str` name, or `&group::Group`); a numeric gid or a name must already exist in `/etc/group` (vision 6.7), as `useradd -g` itself requires. Reverse: accept only `u32`.
- [M6-ug] 2026-09-08 `group::Absent` fails at `check` when the group is some user's primary group (`groupdel` would refuse in `apply` with a less clear message). Reverse: let `groupdel` fail.
- [M6-ug] 2026-09-08 `user::Membership` is satisfied when the group lists the user or is the user's primary group; adds with `usermod -aG` (BusyBox `addgroup user group`), never removes. `Membership::of(&Account)` / `of_name(..)` and `in_group(&Group)` / `in_group_named(..)` both exist so playbooks can chain from ops or use plain names.
- [M6-ug] 2026-09-08 `/etc/passwd` and `/etc/group` are read directly, as the vision plans; NSS-only accounts (LDAP, SSSD, homed) are not seen, same known limitation as `ssh::authorized_keys`. A line that names the requested user or group but does not parse is an error, not "missing".
- [M6-ug] 2026-09-08 `ssh::authorized_keys::{Present, Absent}::for_user(&user::Account)` (the vision 6.1 shape) added on the lead's instruction as one-line wrappers over `for_account(home, uid, gid)`; the only edit to another op in this branch.
- [M6-ug] 2026-09-08 `user::Present` with no `.groups(..)` leaves supplementary memberships alone whatever `append` says; an explicit empty set with `.append(false)` strips them. Reverse: treat "no groups" as the empty set again.
- [M6-ug] 2026-09-08 `Tools::of` probes for `usermod`/`useradd` and then `busybox`/`adduser` before falling back to the distro name, so `apk add shadow` on Alpine (and BusyBox-only images of other distros) are handled by what is actually installed. Reverse: decide by distro only.
- [M6-ug] 2026-09-08 A relative or empty home is refused at `check` in `user::Present` and in `ssh::authorized_keys`' passwd lookup: `useradd` accepts a relative `-d` and writes it into passwd verbatim, which later resolves against the caller's cwd. Reverse: pass it through.
- [M6] 2026-09-08 The `file` brief asked for `symlink`/`read_link` backend primitives; `read_dir(p) -> Vec<PathBuf>` was added as a third, because `file::Absent` must tell an empty directory from a non-empty one at check time and there is no listing primitive (7.3: one method per primitive, no `ls` through `cmd`). Reverse: drop `read_dir` and make `Absent` always recursive.
- [M6] 2026-09-08 `file::Copy` builder order follows the vision 5.6 example: `Copy::from_bytes(..).to(dest)` finishes the builder and `.mode()/.owner()/.backup()` sit on the finished op (as `Directory` does), rather than before `.to`. Reverse: move the three setters onto `CopyBuilder`.
- [M6] 2026-09-08 When a `Copy` changes both content and attributes, the report shows the content diff only (`Diff` has no shape carrying both); `apply` still sets the attributes. An attributes-only change shows `Diff::Attrs` and does not rewrite the file. Reverse: add a combined `Diff` variant in the SDK.
- [M6] 2026-09-08 `file::Attrs` refuses a symbolic link instead of following it, because `stat` is `lstat` and `chmod` follows, which would make every run report a change on the link's 0777. `file::Symlink` refuses to replace a directory even with `.force(true)` (Ansible refuses too) and `.force` only covers a regular file. Reverse: use `stat_follow` in `Attrs` and allow directory replacement behind `.force`.
- [M6] 2026-09-08 `file::Block` with an empty block removes the managed block (Ansible `blockinfile state=absent`) instead of a separate `Block::Absent` type: the marker is the identity and the empty content is a natural "nothing between the markers". `plan_block` takes the first BEGIN and the first END after it; an orphan BEGIN counts as no block (Ansible takes the last of each and swaps). Reverse: add `file::BlockAbsent`.
- [M6] 2026-09-08 Observation for the SDK, not changed here: `Local::write` creates a new file at 0600 (tempfile's default) when nothing existed, so `Copy` and `Line::create(true)` without `.mode()` produce 0600 files where Ansible would give `0666 & ~umask`. The op tests cannot see it (the Fake creates 0644). Suggest `Local::write` chmod new files to 0644 or apply the umask.
- [M6-file] 2026-09-08 `Backend::remove` is single-entry (empty dir or file); `remove_all` is the only recursive delete. `rename` added for atomic replacement. New files from `Local::write` get 0666 minus umask. The fake resolves symlinks for reads and `stat_follow`; a write at a link's path replaces the link (mirrors Local). `Line`/`Block` refuse symlinked paths. Reverse: per item, see the M6-file-ops report self-review.
- [M6-sd] 2026-09-08 `systemd` state ops run both probes (`is-enabled`, `is-active`) in `check` and refuse a unit `is-enabled` reports as not found (`not-found` on systemd 253+, empty stdout with a non-zero exit before), because `is-active` answers `inactive` for a unit that does not exist and `Stopped` would otherwise be silently satisfied on a typo. Reverse: drop the `NotFound` bail in `probe_existing`.
- [M6-sd] 2026-09-08 `systemd::Disabled` refuses `generated` and `transient` units with the same message as `static`: all three have no `[Install]` section, `systemctl disable` exits 0 on them without changing anything, and the op would report `changed` on every run. `alias`, `indirect` and `linked` go through `systemctl disable` as Ansible does. Reverse: shrink `EnabledState::cannot_be_disabled` to `Static`.
- [M6-sd] 2026-09-08 `systemd::Enabled` and `systemd::Running` refuse a `masked` unit with a message naming `systemctl unmask` instead of unmasking (vision 6.7: one resource per op); `Disabled` treats `masked` as satisfied since the brief counts it as not enabled. Reverse: add an `.unmask(true)` builder.
- [M6-sd] 2026-09-08 `EnabledState::is_enabled` follows `systemctl is-enabled`'s own exit-0 table (`enabled`, `enabled-runtime`, `alias`, `static`, `indirect`, `generated`, `transient`); `linked`/`linked-runtime` count as not enabled, as systemctl exits 1 on them. Reverse: move `linked` across.
- [M6-sd] 2026-09-08 After `systemctl enable`/`disable` the op re-reads `is-enabled` and fails if the word did not flip (systemctl exits 0 with only a warning for a unit it cannot enable), so a step never reports `changed` for a no-op. Reverse: drop the two `ensure!`s in `apply`.
- [M6-sd] 2026-09-08 `Fake` was not extended with sequenced command responses; state-op apply tests use one fake for `check` and a second for `apply` because the post-apply probes share their argv with the check probes. A `with_cmd_seq` on `Fake` would make those tests single-fake; left for the lead to decide since two sibling branches already touch `fake.rs`. Reverse: n/a.
- [M6-so] 2026-09-08 `apt::Present::update_cache(bool)` became `update_cache(Duration)` (vision 6.8 writes `update_cache(Duration::from_secs(3600))`); there is no `update_cache_always()`, `Duration::ZERO` reads well enough. Reverse: re-add the bool form as a second method.
- [M6-so] 2026-09-08 The apt lists' age is read with `stat -c %Y` through `sys.cmd` (`/var/lib/apt/lists`, then `/var/cache/apt/pkgcache.bin`; unknown age counts as stale) because the SDK's `Stat` carries no mtime and the brief forbids touching the SDK; branch m6-file-ops is already editing `backend/mod.rs`, so adding a field there would conflict. Reverse: add `mtime` to `Stat` and read it with `sys.stat`.
- [M6-so] 2026-09-08 `apt::Latest` refreshes the cache in `apply` only (vision 6.8 places `apt-get update` in `apply`), so a `Latest` whose `check` finds every package current against stale lists reports `ok` without refreshing. Ansible refreshes in check mode too. PROPOSED AMENDMENT for Cadu: let `Latest::check` run `apt-get update` when the lists are older than `max_age` (a command, not a file mutation under 7.3). Not changed; the module docs say to run `apt-get update` via `shell::Command` first when a box never refreshes on its own.
- [M6-so] 2026-09-08 All apt ops, `Present` included, refuse when `!sys.is_root()` (the brief: "all ops refuse when not root"); a non-root dry run of `apt::Present` therefore fails at `check` instead of reporting would-change. Reverse: drop the root check from `require_apt_root` for `Present`.
- [M6-so] 2026-09-08 `apt::Absent` fails on a package in a dpkg state other than installed, config-files or unknown (half-configured, unpacked, ...) instead of guessing; `dpkg --configure -a` is a human's call. Reverse: treat any known state as present.
- [M6-so] 2026-09-08 `apt::Latest` has `.install_recommends(bool)` (default off, `--no-install-recommends` on the install path) mirroring `Present`, so the two ops install the same set for a missing package. Reverse: remove the method.
- [M6-so] 2026-09-08 `hostname::Is::check` reads `/proc/sys/kernel/hostname` through `sys` and only falls back to `facts.hostname` when that file is unreadable: facts are gathered once at startup, so a second step in the same run would otherwise see the old kernel name and report `changed` twice. Reverse: use `facts.hostname` only.
- [M6-so] 2026-09-08 `hostname::Is` validates the name in `check` (not in `new`, which cannot fail) and accepts dotted FQDNs up to 253 characters with 63-character labels; uppercase is allowed (`HOME-GAMES` is the brief's own example). Reverse: reject dots or fold case.
- [M6-so] 2026-09-08 `sysctl::Present` uses its own line planner (`plan_sysctl_line`) instead of `file::plan_line`: `key=value` and `key = value` must count as the same line, duplicates are collapsed onto the first occurrence, and values compare token-wise (`4096 131072 6291456` equals the kernel's tab-separated form). Reverse: replace with `plan_line` and an escaped-key regex, accepting whitespace-only rewrites.
- [M6-so] 2026-09-08 `sysctl::Present` refuses when the drop-in's directory does not exist (vision 6.7: prerequisites are not created silently; `/etc/sysctl.d` exists on every Debian) and, with `apply_now` on, when `/proc/sys/<key>` is missing; with `.apply_now(false)` a missing key is persisted only and `previous_live` is `None`. Reverse: `mkdir_all` the parent; skip the live write silently.
- [M6-so] 2026-09-08 `sysctl::Present::apply` re-plans the file text from the current file instead of carrying it in the diff: `Diff::Attrs` is used so the file and live changes render as one table, and re-reading a small file is cheaper than a second diff variant. Reverse: emit `Diff::Text` and take `after` from it.
- [M6-so] 2026-09-08 `apt::Present` predicts its output only when `apt-cache policy` can name a candidate version for every missing package; otherwise the plan carries the diff with no prediction (vision 12). A stock image with no lists therefore reports "would install" without inventing a version. Reverse: predict with an empty version string.
- [M6-so] 2026-09-08 `apt::Present::apply` installs the names carried in the diff that `check` produced (vision 6.2) and refuses a plan that names none, instead of reading them back out of the prediction; the report is rebuilt from dpkg after the install. Reverse: derive the install list from `change.predicted`.
- [M6-so] 2026-09-08 `hostname::Is` refuses a name longer than 64 characters (the kernel's `HOST_NAME_MAX`, and what systemd accepts), not 253 (DNS). Reverse: raise the limit back to 253 and let `hostnamectl` reject it.
- [M6-sd] 2026-09-08 PROPOSED AMENDMENT for Cadu: `systemctl daemon-reload` is only reachable as the `.daemon_reload(true)` flag on `Restart` and `Reload` (vision 6.4's shape), so a playbook that writes a unit file and wants systemd to notice it must restart or reload some unit. Ansible allows `daemon_reload` alone. A `systemd::DaemonReload` action (always changes, no unit) would be a ~30-line addition; not built, because the brief and vision name six ops.
- [M7] 2026-09-08 CI runs four jobs: the fmt/clippy/test/doc gate, an MSRV check pinned to the manifest's 1.88, a build of `examples/workspace` (excluded from the cargo workspace, so nothing else compiles it), and the Docker harness with `RUSTIBLE_INTEGRATION=1`. Reverse: fold everything into one job.
- [M7] 2026-09-08 `cargo doc` in CI passes `--lib`: documenting the `rustible` facade crate and rustible-cli's `rustible` binary together collides on `target/doc/rustible/index.html`. Reverse: rename the binary's doc target or drop the doc gate.
- [M7] 2026-09-08 The release workflow patches the version with two seds over the root manifest (the `[workspace.package]` line and the six `[workspace.dependencies]` entries) and fails if any `0.0.1` remains, rather than taking a dependency on cargo-edit. Reverse: `cargo set-version` with cargo-edit installed in the job.
- [M7] 2026-09-08 Each `cargo publish` retries up to eight times, 30 s apart, and treats "the registry already lists this version" as success, so crates.io index lag between the seven crates cannot fail a release and a re-run of the job is safe. Reverse: publish once and let the job fail on lag.
- [M7] 2026-09-08 Release binaries are built with `--profile dist` and no musl-tools: `rust-lld` plus rustup's self-contained musl crt, matching `.cargo/config.toml`. Reverse: install musl-tools and use the GNU linker.

- [M3] 2026-09-08 `--limit host,group` narrows the playbook's `hosts` (Ansible's semantics) instead of replacing them; an item outside the playbook's hosts is a usage error listing them. The brief says "hosts from the attribute (or `--limit`)", which reads either way; narrowing cannot run a playbook on a host its author did not target. Reverse: make `--limit` the target set in `run::select_targets`.
- [M3] 2026-09-08 `ssh_user` and `port` are passed to `ssh` only when the inventory sets them (host, group, or `defaults`); the built-in defaults (local username, 22) are ssh's own defaults, so leaving them out lets `~/.ssh/config` win (vision 5.4 "inherits ~/.ssh/config"). Reverse: drop the `Source::BuiltIn` check in `run::ssh_target`.
- [M3] 2026-09-08 The SSH ControlMaster is launched by the CLI (`ssh -M -f -N -S <socket> ... <ssh_args> <addr>`, `ControlPersist=300`, `StrictHostKeyChecking=accept-new`) and handed to `openssh` through `Session::resume`, because `openssh`'s builder cannot pass arbitrary arguments and the inventory has `ssh_args` (vision 10.2.1). Reverse: use `SessionBuilder` and refuse non-empty `ssh_args`.
- [M3] 2026-09-08 Exit codes: 0 ok; 1 the inventory, the vars pre-check, or a cargo build failed (nothing ran); 2 a host failed (connect, upload, protocol, or a step); 3 the command line was wrong (clap errors included, so a usage error is never mistaken for a failed host). Reverse: n/a, additive.
- [M3] 2026-09-08 The describe cache holds only the `--describe` JSON; the `--check-vars` pre-check needs the host-native binary, so on a cache hit the existing debug binary is asked `--describe` (milliseconds) and used when it is a selected build of that playbook with the identical entry, else rebuilt. A playbook without vars never needs the binary after the first run. Reverse: always run `cargo build` before the pre-check (a fresh no-op build costs a build-script re-run when the selected playbook changed).
- [M3] 2026-09-08 `--check-vars` reports structured problems from the schema walk (`non_flat_vars`, `missing_required`, `type_mismatches` as errors, `unknown_keys` as warnings) and then, when the walk is clean, runs serde on the typed struct; a serde-only failure is reported with an empty `var`. Serde is the authority (vision 10.3), the walk attributes. `vars::unknown_keys` reports nothing for a `null` schema (a no-vars playbook used to warn about every inventory var at `Start`). Reverse: n/a.
- [M3] 2026-09-08 `inventory check` builds the whole workspace once (no `RUSTIBLE_PLAYBOOK`, every playbook, the editor's build) and runs `--describe` and `--check-vars` on that binary, instead of one selected build per playbook; undeclared vars print as `warning:` lines. Reverse: loop over selected builds.
- [M3] 2026-09-08 The runtime's local mode (`<binary> <name>`) prints one line per event through `event::Compact`, or JSON lines with `--json`; the buffered renderer lives in the CLI only (vision 9). Reverse: n/a.
- [M3] 2026-09-08 `--json` prints one object per line, `{"host", "frame"}` for every `Up` frame plus `{"host", "error"|"stderr"|"exit"}` for what the orchestrator adds; a playbook that says `escalate` on a host with `escalate="none"` runs unescalated with a `-v` note (the playbook's own root check reports it). Reverse: n/a.
- [M3] 2026-09-08 `examples/workspace/hosts.kdl` group `lab` gained `vars { package "mc" }` so the brief's done-when runs `cadu/mc` without `--var`. Reverse: n/a.
- [M3] 2026-09-08 Merging main brought the Docker test harness (`rustible_sdk::testing`), which printed through the `Pretty` sink M3 deleted from the SDK; it now uses `Compact`, the SDK's remaining local printer, without the image name prefix (a container runs exactly one host, and libtest already prints the test name). Reverse: give `Compact` an optional label, or move a host-prefixed printer back into the SDK.
- [M3] 2026-09-08 `examples/workspace/playbooks/cadu/mc.rs` keeps its `update_cache: bool` var (inventories and `--var update_cache=true` from M1/M2 still work) and maps `true` to `apt::Present::update_cache(Duration::ZERO)` after PR 9 changed the method's argument to a `Duration`; the example workspace is not a cargo workspace member, so `cargo test --workspace` cannot catch such breaks and only the done-when run did. Reverse: make the var an age in seconds and pass `Duration::from_secs`.
- [M3] 2026-09-08 The remote upload writes to `<path>.$$.tmp` (the remote shell's pid) and removes it on failure, because the final path is a content hash shared by concurrent runs and `exists` only tests for an executable file, so a mixed temp file moved into place would be a permanent cache hit. Reverse: go back to a fixed `.tmp` name.
- [M3] 2026-09-08 The CLI's frame reader caps a frame at 64 MiB, the SDK's ceiling, and reports a protocol error naming `ctx.log`: a playbook that writes to stdout desyncs the stream and any four bytes then read as a length. Reverse: allocate whatever the length says.
- [M3] 2026-09-08 Everything between spawning the playbook binary and its stdout EOF runs in `drive_frames`, whose caller attaches the child's stderr to any error, so `sudo -n` being denied reads as sudo's message instead of `Broken pipe`. Reverse: inline the loop again.
- [M3] 2026-09-08 `pick_bin` chooses the `src/main.rs` bin target, falls back to a lone bin elsewhere, and refuses a workspace with several by name; cargo metadata promises no target order, so taking the first `bin` could silently drive a user's `src/bin/tool.rs`. Reverse: take the first bin.
- [M3] 2026-09-08 The global `--workspace` resolves a relative path for `playbook create` and is a usage error for `init` (whose directory is its argument), rather than being accepted and ignored. Reverse: ignore it again.
- [M3] 2026-09-08 A build or artifact-read failure after the hosts are connected is reported against every host and closes their transports rather than returning early, so the run still prints its summary and no ssh ControlMaster is stranded. Reverse: propagate with `?`.
- [M3] 2026-09-08 KNOWN GAP (review, not fixed): the describe cache key hashes the playbook source and `Cargo.lock` only, so editing a path dependency (the example workspace and dogfooding both use them) does not invalidate it. No observable failure could be constructed today, because `check_binary` re-describes and rebuilds on a mismatch and the `Hello` check catches protocol drift. Close it by hashing the resolved path-dependency sources or a build fingerprint.

- [M6-sh] 2026-09-08 `Op` gained `changed_by_apply(&self, &Output) -> bool` (default `true`), asked by `Ctx::step` after a successful `apply`; `false` finishes the step `Ok` with the diff kept and the note "ran, unchanged". This is what `shell::Command::changed_when` needs: an action can only tell after running whether anything happened, and `check` must not run the command (a dry run would execute it). Never consulted in check mode, where such a step reports `would change`. Reverse: drop the method and make `changed_when` a plain post-step `if` in the playbook.
- [M6-sh] 2026-09-08 `shell::Command::changed_when` takes a closure stored as `Arc<dyn Fn(&CommandOutput) -> bool + Send + Sync>`, with a hand-written `Debug` (`"<closure>"`, and stdin plus every env value shown as a byte count, so neither a piped secret nor a `PGPASSWORD` lands in a log; env names and argv stay visible, argv because it reaches the process table anyway); `Clone` still derives. A function pointer or predicate enum was not needed. Reverse: n/a.
- [M6-sh] 2026-09-08 `shell::Command::sh(script)` is `/bin/sh -c <script>`, the `ansible.builtin.shell` mapping; `new(program)` stays the `command` mapping. `always_changes()` is false once any of `creates`, `removes` or `changed_when` is set. Reverse: remove `sh`.
- [M6-sh] 2026-09-08 `stdin`/`env` already existed on `Cmd`/`CmdSpec`/`Local::spawn` and the `Fake` records them in `commands()`; the op exposes them. `Local::spawn` now writes stdin from a thread while draining stdout/stderr, so a child that writes more than a pipe buffer before reading its input cannot deadlock (`cat` with 4 MiB is the unit test); EPIPE from a child that never reads is ignored, the exit status tells the story. Reverse: write stdin inline again.
- [M6-sh] 2026-09-08 Check mode and planned prerequisites (PR 6 review finding): `System` gained a planned-resource list shared by every clone of a run (`note_would_create(kind, name, id)`, `would_create(kind, name)`, `would_create_id(kind, id)`; the last two answer `false`/`None` outside check mode). `group::Present::check` notes the group it would create; `user::Present` (`groups`, `gid` by name or gid) and `user::Membership` accept a planned group in check mode and report `would change`. Nothing is created (vision 6.7 holds) and a real run never accepts a group that is not on the machine, so "refuse rather than guess" is intact: the dry run only trusts what an earlier step in the same run said it would do, exactly as the real run trusts that step having done it. A planned group whose gid is unknown appears in the user diff as `group=<name>` and blocks prediction (vision 12); with `group::Present::gid` the gid is known and `.gid(&grp)` predicts. Reverse: delete the three `System` methods and the `would_create` branches; the dry run of vision 6.6 then aborts at the user step again.
- [M6-sh] 2026-09-08 `user::Present` on BusyBox does not predict (or show in the diff) a shell it was not given: BusyBox `adduser` uses `$SHELL`, else the invoking user's own passwd shell (`/bin/ash` for root on Alpine), else `/bin/sh`, and the op cannot see that environment through `sys` (and under sudo or the M5 helper it differs from the playbook's). An explicit `.shell()` is predicted as before; the previous rule only did this for `system(true)` accounts. Alternative rejected: pin `SHELL=/bin/sh` in the `adduser` environment to make `/bin/sh` true, because it would silently give accounts a different shell from a hand-run `adduser`. Reverse: predict `/bin/sh` again.
- [M6-sh] 2026-09-08 Container tests use distinct user and group names (`rustible-usr`, `rustible-grp`): on Debian `useradd` creates a private group named after the user and refuses when a group of that name already exists, so the brief's literal "`Present::new("rustible-test")` for both" cannot run. Not a code change. Reverse: rename them back to one shared `rustible-test` and the Debian legs fail at `useradd`.
- [M6-sh] 2026-09-08 Found by the container test: `group::Present::new(x)` then `user::Present::new(x)` (the vision 6.1 shape with the group first) fails on Debian with "group x exists - if you want to add this user to that group, use -g", and BusyBox `adduser` dies with "group name 'x' is in use", because both try to create the private group. `user::Present` without `gid` now uses a group named after the new account as its primary group (`useradd -g <gid>`, `adduser -G <name>`), which also makes the gid predictable; in check mode a planned same-named group is accepted the same way. Ansible passes `-N` instead and lands the account in the default group (`users`, gid 100), which nobody asked for. An explicit `gid` still wins. Reverse: drop `same_named_group` and let the tool fail.
- [M6-sh] 2026-09-08 `System::would_create_id_by_name(kind, name)` added next to `would_create_id(kind, id)` so an op can learn the gid an earlier step planned for a group it knows by name. Additive. Reverse: delete the method; `user::Present` then falls back to the by-name branch that blocks prediction.
- [M6-sh] 2026-09-08 `testing::wait_for_systemd` no longer treats `offline` as a terminal state (`stopping` and `maintenance` still are). `systemctl is-system-running` answers `offline` whenever it cannot reach the manager, which includes the first fraction of a second of a container's life, so `it_systemd_image` failed on `jrei/systemd-ubuntu:24.04` about one run in ten with "last state: `offline`" while the same test passed three times in a row after. A dead container is still caught immediately by the `container_running` probe; the only cost is that a non-systemd image now takes the full 60s timeout to be diagnosed instead of failing fast. Found while re-running the whole container suite for this branch; the harness is main's code, not this branch's. Reverse: put `"offline"` back in the terminal arm.
- [M6-sh] 2026-09-08 The BusyBox findings get their own container test, `it_user_busybox` on `alpine:3.20`, instead of an Alpine leg on `it_user_group`: BusyBox is a different toolset (no `usermod` at all), so the Debian test's assertions about `/bin/bash`, `USERGROUPS_ENAB` and `usermod` do not have Alpine equivalents to branch to. It proves the shell finding rather than asserting it: the same `adduser -D` gives `/bin/sh` with no `$SHELL` and `/bin/ash` under `SHELL=/bin/ash`, on one image, in one run. Reverse: delete the file; the BusyBox paths fall back to `Fake` coverage only.

- [M6-gh] 2026-09-08 `UserKeys` fetches `https://github.com/<login>.keys`, not the REST API's `/users/<login>/keys`: the plain-text endpoint needs no token, no JSON parsing and no dependency, and is not under the API's 60-per-hour anonymous limit; the API adds only key ids, which nothing here consumes. Reverse: swap the URL and parse JSON, adding a `serde_json` dependency and a token knob for the rate limit.
- [M6-gh] 2026-09-08 The HTTP call sits behind a crate-local `Fetch` trait (`Https` the default) rather than a new `System`/`Backend` primitive: `System` models the target machine's files and processes (vision 7) and the SDK deliberately has no network, so a collection that needs one owns its seam. This is the shape a third-party collection should copy. Reverse: add a `get` primitive to `Backend` and drop `Fetch`.
- [M6-gh] 2026-09-08 `keys_to_user` / `KeysToUser` is **additive** by default; `.exclusive(true)` is opt-in and Ansible's `exclusive` default (false) agrees. Additive can never lock anyone out, which is the failure mode that matters for `authorized_keys`. Reverse: flip the default in `KeysToUser::new`.
- [M6-gh] 2026-09-08 In exclusive mode an empty key list is a hard error, not an empty `authorized_keys`: a user deleting their GitHub keys must not lock the account. Ansible would truncate. `ssh::authorized_keys::Present` is still reachable directly for anyone who really wants it. Reverse: drop the `bail!` and let `Present` truncate.
- [M6-gh] 2026-09-08 Keys are installed with the comment `github:<login>` (the `.keys` endpoint strips comments), overridable with `.comment(..)` or `.without_comment()`. Comments never take part in matching, so changing it does not rewrite existing lines. Reverse: default `comment` to `None`.
- [M6-gh] 2026-09-08 A 404 fails the step naming the user, while a 200 with an empty body is a successful empty `Vec`: a typo must not silently install nothing, but a real user with no keys is a legitimate state. A body line that is not a public key fails rather than being skipped, since a dropped key under `exclusive` is a lockout. Reverse: map 404 to an empty list and skip unparseable lines.
- [M6-gh] 2026-09-08 The login is validated against GitHub's rules (1-39 chars, ASCII alphanumerics and single interior hyphens) in `check`, before any request, so a playbook typo fails locally with a message instead of as a 404. Validation is in `check`, not `of()`, because constructing an op cannot fail (vision 6.2). Reverse: delete `validate_login` and let GitHub answer.
- [M6-gh] 2026-09-08 `KeysToUser` is a plain helper running two `ctx.step`s, not an `Op`: the fetch and the install stay visible as separate lines in the run report. This is the recommended way for a collection to compose without hiding work. Reverse: make it a single `Op` whose `check` fetches.
- [M6-gh] 2026-09-08 `ureq` is taken with `default-features = false` plus `rustls-no-provider` and `rustls-webpki-roots`, and the crypto provider is `rustls-rustcrypto`: ureq's default rustls feature pulls `ring`, which is C (vision 5.3 forbids it). Trust roots are the bundled webpki set, so no system certificate store is read. Note `rustls-rustcrypto` is `0.0.2-alpha` and slower than ring; it is the only pure-Rust provider today. Reverse: enable ureq's default rustls feature and accept `ring`.
- [M6-gh] 2026-09-08 The default client caps a response body at 1 MiB and uses a 20-second connect/read timeout (`.timeout(..)` overrides), and sends a `rustible-github/<version>` user agent because GitHub rejects requests without one. Reverse: raise or drop the cap in `fetch::MAX_BODY_BYTES`.
- [M6-gh] 2026-09-08 Exactly one test crosses the network and it is `#[ignore]`d, so `cargo test` is deterministic and offline everywhere including CI; run it with `cargo test -p rustible-github -- --ignored`. Reverse: none wanted.
- [M6-gh] 2026-09-08 PROPOSED AMENDMENT for Cadu, security-relevant: the only pure-Rust rustls crypto provider today is `rustls-rustcrypto`, published at `0.0.2-alpha`, and vision 5.3's no-C rule makes it the only option for any op that speaks TLS (`github::UserKeys` here, `http::Download` in the sibling branch). That means SSH public keys are fetched, and files are downloaded, through an alpha TLS implementation. The alternatives are: accept it knowingly for now, keep the network ops but mark them experimental, or drop network ops until a mature pure-Rust provider exists. Not decided by an agent. Reverse: enable ureq's default rustls feature, which pulls `ring` (C) and breaks the pure-Rust rule.

- [M6-sh] 2026-09-08 Review finding: because `user::Present` adopts a same-named group as the new account's primary group (`useradd -g`), `user::Absent` can later remove that group, since `userdel` takes a primary group that shares the account's name and has no other members (`USERGROUPS_ENAB`, the Debian and Ubuntu default). So `group::Present::new("app")`, `user::Present::new("app")`, `user::Absent::new("app")` deletes group `app` although no step asked for it. Accepted rather than worked around: the alternative is Ansible's `useradd -N`, which suppresses the private group and lands the account in the system default group (`users`, gid 100), and a surprising group membership is worse than a surprising group removal that the account's own name invited. Documented in the `user::Absent` rustdoc under "A group named after the account can go with it", with the workaround (give the group another member or another name); `it_user_group` asserts the outcome. Reverse: pass `-N` in `Present::create` when the primary group came from `same_named_group` rather than from an explicit `gid`, and drop the rustdoc section.
