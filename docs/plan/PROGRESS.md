# Progress

State per milestone: `todo` | `in-progress (branch)` | `pr-open (#n)` | `merged` | `blocked (why)` | `deferred`.
Updated by whoever is working; read first on every resume.

| Milestone | State | Notes |
|---|---|---|
| M1 foundation | merged | PR #1, 2026-09-08; ARM VM leg deferred (VM unreachable for an hour), see logs/M1-arm.txt |
| M2 inventory | merged | PR #4, 2026-09-08; review: 10 fixes applied by the lead; --check-vars recommendation recorded for M3 |
| M3 real run | merged (#10) | workspace discovery, playbook run/list, inventory check, the vision 5.2 pipeline, the CLI renderer, spikes deleted; done-when block verified on local and the ARM VM four times, the last after six review fixes; all four CI jobs green |
| M4 init | merged | PR #2, 2026-09-08; review: 10 findings, 9 fixed, 1 recorded as proposed amendment |
| M5 elevated/cancel/streaming | merged (#12) | Elevated helper (all fifteen Backend primitives), Cancel, file and secret streaming, fetch, protocol 3; reworked onto M3 and re-verified through the real CLI on local and the ARM VM; two review rounds, ten findings fixed, including three silent no-op kills and a temp-directory leak |
| M6 stdlib wave one | done | every op in the brief merged: ssh::authorized_keys (#3), user+group (#6), file family (#7), apt Absent/Latest + hostname + sysctl (#9), systemd (#8), rustible-github (#14 with review fixes in #17), shell extras and container tests for every merged op (#16), http::Download and archive::Extracted (#15). 13 container test files across debian:12, ubuntu:24.04, alpine:3.20 and two systemd images |
| M6 rustible-github | todo | |
| M6 docker harness | merged | PR #5, 2026-09-08; review: 7 fixes, 3 dismissed with reasons; `rustible_sdk::testing` + `#[rustible::integration_test]` |
| D dogfood | human-only | never unattended |
| M7 release prep | done except the release | CI and release workflows (PR #11); README (PR #13); three amendments (PR #19), with the vision doc itself amended to match in PR #24; TLS on rustls-graviola (PR #20), then on `ring` with rustup+clang as the toolkit and vision 5.3 amended again (PR #26, open, MSRV back to 1.88); rustdoc on every public item, enforced with deny(missing_docs) (PR #21). Only the release itself is left, and it is Cadu's |

## Log
(append one line per event: date, milestone, what happened)
- 2026-09-08 00:55 UTC  M1  started on branch m1-foundation (unattended run 1, merge=yes)
- 2026-09-08 02:55 UTC  M1  merged as PR #1 after code-review (10 findings, 9 fixed, 1 dismissed with reason); ARM VM leg deferred
- 2026-09-08 03:00 UTC  M2,M4,M5,M6-harness  started in parallel on their own worktrees (four subagents)
- 2026-09-08 03:45 UTC  M4  merged as PR #2 after code-review (10 findings, 9 fixed by the lead, 1 proposed amendment in DECISIONS.md)
- 2026-09-08 02:49 UTC  all   the four subagents (M2, M5, M6-harness, M6-authorized-keys) were killed by an account session rate limit; work preserved in their worktrees (M2: code+log committed; M5: code committed, report drafted; harness: code committed, log written; authorized-keys: just started)
- 2026-09-08 10:20 UTC  all   Cadu re-invoked /goal ("keep going on all fronts"); four resume agents launched against the existing worktrees; ARM VM still unreachable (timeout)
- 2026-09-08 10:32 UTC  M2,M6  PRs #3 (authorized_keys), #4 (inventory), #5 (harness) opened by the resume agents; reviews in progress; user/group and file-ops agents started
- 2026-09-08 10:50 UTC  M6  ssh::authorized_keys merged as PR #3 after review (7 findings, all addressed; ~/.ssh creation reversed per vision 6.7)
- 2026-09-08 11:25 UTC  M2  merged as PR #4 after review (lead consolidated eight finder reports; 10 fixes)
- 2026-09-08 11:30 UTC  M3  started on branch m3-run (subagent); active: M3, M5 (ARM pending), user/group, file ops; PR #5 harness under review
- 2026-09-08 11:45 UTC  M6  user/group PR #6 opened (55 tests + for_user wrapper); systemd agent started; PR #5 harness review in finder phase
- 2026-09-08 11:50 UTC  M6  file ops PR #7 opened (63 tests; SDK symlink/read_link/read_dir primitives); small-ops agent started (apt Absent/Latest, hostname, sysctl)
- 2026-09-08 12:20 UTC  M6  file ops merged as PR #7 after review (10 fixes incl. SDK remove/remove_all/rename and fake symlink semantics)
- 2026-09-08 12:45 UTC  M6  Docker harness merged as PR #5 (container-side marker, Drop guard, timeout, fail-not-skip when enabled; verified on debian/ubuntu and jrei systemd images)
- 2026-09-08 11:08 UTC  M6  user/group merged as PR #6 after review (optional groups, uid-taken guard on existing accounts, private-gid guess dropped, useradd HOME= default, tool probing, absolute-home check); PRs #8 systemd and #9 small-ops open for review
- 2026-09-08 14:53 UTC  M6  small ops merged as PR #9 after review (apt::Present predicts only with a known candidate, apply works from the diff, hostname limited to HOST_NAME_MAX; container tests re-run green)
- 2026-09-08 14:54 UTC  M6  systemd merged as PR #8 after review (no code changes needed; container test re-run green on both jrei images). ARM VM came back up: M3 and M5 agents restarted to run their ARM legs
- 2026-09-08 14:56 UTC  M6,M3,M5  ARM VM back up: agents `m3-arm` and `m5-arm` running the ARM legs on the existing m3-run and m5-elevated-streaming worktrees. M6 wave two started: `m6-net-archive` and `m6-shell-tests`. Lead is on M7 prep (CI and release workflows).
- 2026-09-08 15:08 UTC  M7  prep merged as PR #11: .github/workflows/ci.yml (gate, MSRV 1.88, example workspace, Docker harness) and release.yml (version patch, seven crates in dependency order with index-lag retries, musl + macOS binaries). All four CI jobs passed on the PR itself. Two real breakages fixed on the way: examples/workspace had stopped compiling after the apt update_cache signature change, and cargo doc failed on three bad doc links. README rewrite and the full rustdoc pass wait for M3. NOTE for Cadu: pushing .github/workflows over the https remote is rejected (the gh OAuth token has no `workflow` scope); pushed over ssh instead, or run `gh auth refresh -s workflow`.
- 2026-09-08 15:16 UTC  M3,M5,M6  PR #10 (M3) and PR #12 (M5) open, both with their ARM legs verified. Review running on #10. Merge order is #10 then #12, because M5 and M3 both rewrite runtime.rs, transport.rs and main.rs. M6 wave two in flight: net/archive, shell+container tests, github collection.
- 2026-09-08 15:28 UTC  M3  merged as PR #10 after review (eight findings: six fixed on the branch, two recorded). Fixes: unique upload temp name (a fixed one could publish an interleaved binary as a permanent cache hit), a 64 MiB frame cap, the child's stderr kept on early failures, src/main.rs bin-target selection, the vars report naming the file actually read, and --workspace honoured by playbook create and refused by init. Re-verified on local and the ARM VM
- 2026-09-08 15:32 UTC  M7  README rewritten and opened as PR #13, verified command by command from an empty directory against the merged CLI (init, playbook create, list, inventory check, run). The install section says plainly that the crates.io names are 0.0.1 placeholders and builds from a clone instead.
- 2026-09-08 15:32 UTC  ALL  Four subagents hit the Fable model quota mid-task and were resumed on Opus against their existing worktrees: m5-rework (merge onto M3, re-verify through the real CLI), m6-net-archive-2, m6-shell-tests-2, m6-github-2. Each had already committed its implementation; what remained was verification, reports and PRs.
- 2026-09-08 15:33 UTC  M7  README merged as PR #13 (CI green).
- 2026-09-08 15:37 UTC  M6  rustible-github merged as PR #14, the first collection: github::UserKeys behind a crate-local Fetch seam, keys_to_user additive by default with exclusive refusing to empty an authorized_keys. Offline-ness of the suite proved by blackholing the proxy variables with the ignored network test as the control. OPEN FOR CADU: the pure-Rust rule leaves rustls-rustcrypto 0.0.2-alpha as the only TLS provider, recorded as a proposed amendment
- 2026-09-08 15:38 UTC  M6  FOLLOW-UP OWED: PR #14 (rustible-github) was merged on the lead's own read of the diff (fetch seam, op contract, composition defaults, login validation, decisions) plus green CI, but the multi-finder `code-review` pass the protocol asks for never ran on that branch. Queue `/code-review high 14` once the PR #15 review finishes, and fix whatever it finds on a follow-up branch. Everything else merged today had the full pass.
- 2026-09-08 15:54 UTC  M6  PR #15 (http::Download, archive::Extracted) reviewed: seven findings, all real, being fixed on the branch. The two that matter: `archive` sized its read buffer from the tar header's own size field, so a crafted archive claiming 2^62 bytes aborts the process instead of producing the honest refusal; and chmod ran before chown in both `archive::write_member` and the shared `file::apply_attrs`, so extracting a setuid binary with an owner silently lost the bit (that second one is a bug in already-merged file-ops code).
- 2026-09-08 15:54 UTC  M6  PR #16 (shell extras, container tests for the merged ops, the two PR-#6 follow-ups) reviewed: four findings, all being fixed. `shell::Command`'s hand-written Debug redacts stdin and then prints env values in full; an Alpine test's prediction assertion cannot fail because integration bodies run outside check mode; the named form of a planned group loses its gid where the by-value form keeps it; and `user::Absent` can now remove the same-named group a previous step created, which needs documenting. CI green on both branches; the check-mode planned-resource mechanism was verified inert outside check mode.
- 2026-09-08 15:54 UTC  M6  Review debt from PR #14 being cleared: `/code-review high 14` running against the merged github collection.
- 2026-09-08 15:58 UTC  M6  shell extras and the missing container tests merged as PR #16 after review (four findings fixed, two of them verified to fail against the pre-fix code). No TODO(M6 harness) comment remains in crates/. Eleven container binaries now run across debian:12, ubuntu:24.04, alpine:3.20 and the two systemd images. The container tests earned their keep twice: they caught that group::Present then user::Present with one name fails on every real image (useradd and BusyBox adduser both try to create the private group), which no Fake test could see, and they surfaced a flake in main's own harness where 'offline' was treated as terminal though systemctl answers it whenever it cannot reach the manager
- 2026-09-08 16:19 UTC  M6  Review debt on PR #14 cleared and its findings merged as PR #17. The real one: a `.keys` response carrying an `authorized_keys` options field was installed verbatim, so a proxy, an Enterprise host, a caching layer or anyone who could answer for github.com could put `command="..."` into a target's authorized_keys and the run would report a clean `changed`. Refused now, comments stripped, tests at the parser and through the op. Five more: the label could be silently ignored, a fresh ureq Agent per request re-parsed the root store, the documented GitHub Enterprise hatch had no API (there is a base_url builder now), a dead rustls dependency, and byte-counted login validation. The alpha TLS provider's own README ("DO NOT USE THIS IN PRODUCTION") is now recorded against the open amendment for Cadu.
- 2026-09-08 16:23 UTC  M6  http::Download and archive::Extracted merged as PR #15 after review (seven findings, all fixed). Two were security bugs: a tar header's own size field sized the read buffer, so a crafted archive aborted the process through apply instead of being refused; and chmod ran before chown, so extracting a setuid binary with an owner silently lost the bit, which was also true of the shared file::apply_attrs that every merged file op uses. Both re-verified by putting the bug back and watching the container tests fail. M6 is complete
- 2026-09-08 17:08 UTC  M6  FOLLOW-UP OWED (small, main's code): `stdin_reaches_the_child_and_a_large_input_does_not_deadlock` in `crates/rustible-sdk/src/backend/local.rs` has no time bound, so a regression of the stdin-from-a-thread fix hangs the test instead of failing it, and in CI would wedge the job until the workflow timeout. The M5 agent found this while checking its own helper-boundary test, which is bounded by a channel timeout and fails in thirty seconds. Bound main's the same way.
- 2026-09-08 17:08 UTC  M6  FOLLOW-UP OWED (small, main's code): `stdin_reaches_the_child_and_a_large_input_does_not_deadlock` in `crates/rustible-sdk/src/backend/local.rs` has no time bound, so a regression of the stdin-from-a-thread fix hangs the test instead of failing it, and in CI would wedge the job until the workflow timeout. The M5 agent found this while checking its own helper-boundary test, which is bounded by a channel timeout and fails in thirty seconds. Bound main's the same way.
- 2026-09-08 17:14 UTC  M6  Follow-up cleared as PR #18: the `Local::spawn` stdin test is bounded on a worker thread, so a regression of the deadlock fix fails in thirty seconds with a message instead of hanging the CI job until the workflow timeout.
- 2026-09-08 18:04 UTC  M7  three amendments merged as PR #19 after Cadu decided them: rustible init refuses only on a file it would write itself (cargo's rule, so a fresh clone works), apt::Latest refreshes the lists in check because its answer is read from them (the one place --check is not read-only, warned about in the run output), and systemd::DaemonReload exists as an action naming no unit, which removes the journald workaround from our own container test. The vision-doc edits are in docs/plan/VISION-AMENDMENTS-2026-09-08.patch: six hunks with rationale, for Cadu to apply
- 2026-09-08 18:41 UTC  M7  TLS provider swapped to rustls-graviola as Cadu decided, no fallback, merged as PR #20. rustls-rustcrypto is gone from the tree; a CPU pre-flight runs before each HTTPS request (and only for https URLs, so a plain-http download still works on an old machine) and fails the step naming the missing extension rather than panicking mid-playbook. Requirements were read from graviola's source, not its README, which is wrong in both directions. Costs: MSRV 1.88 to 1.89 (graviola declares it, unavoidable), and playbook binaries grow about a third. Wins: the lock drops from 217 crates to 165, rsa 0.9.10 leaves with unpatched RUSTSEC-2023-0071, and the unaudited P-256/P-384 arithmetic is replaced by s2n-bignum. Real handshakes on both targets, the aarch64 one on the ARM VM
- 2026-09-08 18:56 UTC  M5  merged as PR #12 after two review rounds. The kill path was wrong three separate ways, each reporting success while the playbook binary kept running: the binary's path was recovered from argv and picked up '-u' behind a non-root escalate_user; the /proc/exe guard needed ptrace access the login user lacks for an escalated run's processes, so it skipped every candidate; and the pgrep pattern matched the shell running the loop, which killed itself partway through. All three hid behind sshd tearing down the session on exit. Also fixed: cancel could not be sent during a file transfer (the feature was inert for exactly the long operations it exists for), ctrl-c was observed once and only inside the run loop, an unbounded inbox buffered a streamed file whole in RAM, a bounded one then created an abandoned-consumer deadlock, the escalation prefix reached a shell unquoted with escalate_user unvalidated from the inventory, and a killed binary left its temp directory on the target. Every milestone is now merged
- 2026-09-08 19:30 UTC  M7  rustdoc pass merged as PR #21: 564 undocumented public items closed (326 in the SDK alone), now enforced with deny(missing_docs) in all seven library crates. rustible-github was already at zero because it is the only crate that had the lint on, which explains the whole spread. Reading every public item turned up ten behaviour problems, all recorded in docs/plan/reports/M7-rustdoc.md and none touched: the run summary under-counts warnings (only Ctx::warn increments the counter, so a run prints WARNING lines and reports 0), Event::Failed.step is never filled so the renderer string-parses the step name out of the error chain, Status::Skipped is unreachable with live match arms, Line::check reports the wrong line number on a satisfied step with duplicates, Elevated::call detects an oversized response by string-matching an error message, four public items in file are unreachable, and Down::Start.verbosity is dead on the receiving end
- 2026-09-08 19:51 UTC  M7  Five findings from the rustdoc read fixed and merged as PR #23; the other five recorded with what closing each would take. The run summary now counts warnings at the sink rather than at one producer, so all three land in the same column; `Event::Failed.step` is filled from a typed `StepFailed` layer instead of string-parsing the error chain; `Status::Skipped` is gone, with an exhaustive test naming every variant's producer so an unreachable one fails to compile; `Elevated` detects an oversized response by downcasting a typed `FrameTooLarge` instead of matching message text; and `Line::apply`'s satisfied branch no longer reports check's stale prediction. Two corrections worth keeping: finding 5 did not reproduce as written (the check-phase branch cannot disagree with the planner) and the real bug beside it was fixed instead, and finding 7 could not be pinned by a behavioural test, so the coupling was demonstrated by rewording the framing message and watching the refusal degrade.
- 2026-09-08 19:51 UTC  RUN  Unattended run 1 complete. Twenty-three pull requests merged, every milestone in the plan done, main green with 561 tests and no open PRs. Report at docs/plan/reports/RUN-2026-09-08.md. Left for Cadu: cut the release. Nothing was published; all seven crates remain at 0.0.1.
- 2026-09-09 00:44 UTC  DOCS  Review of the vision-amendment commit found three documentation errors, all fixed there: the vision doc claimed `rustible init` leaves an existing `.gitignore` alone when it appends the lines it lacks; the run report's first handover item still told Cadu to apply a patch that commit had deleted; and this file still listed applying it as outstanding, which a resuming agent reads first. Also: the non-apt refusal is shared by all three apt ops, not `Present`'s alone.
- 2026-09-08 22:15 UTC  M7  TLS provider swapped again, graviola to `ring`, on Cadu's decision after `docs/plan/reports/C-TOOLCHAIN-SPIKE.md`. The required toolkit becomes rustup plus clang; the vision 5.3 amendment was written out in `docs/plan/reports/M7-tls-ring.md` and the lead applied it on the branch (a21da4c). Graviola aborted on anything below Intel Broadwell, which broke `github::UserKeys` on the Vultr outpost for want of `adx`; ring detects at runtime and runs to baseline x86-64, demonstrated under qemu at seven CPU models including Haswell. The CPU pre-flight is deleted with it; `tls::provider()` survives. New: `rustible_cli::toolchain` refuses a build that needs clang with the package name instead of cc-rs's `aarch64-linux-musl-gcc`, `rustible init` warns, and musl 1.2.5's x86_64 headers are vendored (MIT) and embedded so `cargo install rustible-cli` is enough. MSRV comes back down to 1.88. Playbook binaries shrink 22% on both musl targets. Open as PR #26, not merged; the lead applied the vision 5.3 amendment on the branch
- 2026-09-09 01:08 UTC  M7  TLS moved from rustls-graviola to ring and merged as PR #26, after a real host in the dogfood fleet (the Vultr outpost) could not do HTTPS at all: graviola asserts instruction set extensions and aborts below Intel Broadwell. Cadu decided the toolkit becomes rustup plus clang, and vision 5.3 is amended to match: no toolchain stock rustup cannot drive, no cross-gcc, no zig, no docker, clang on the operator's machine only, target hosts still need nothing. Measured in docs/plan/reports/C-TOOLCHAIN-SPIKE.md, which is now tracked. Results: the CPU floor is gone (verified under qemu from qemu64 upward, where graviola refused everything through Haswell), playbook binaries are 22 to 24 percent smaller, MSRV comes back to 1.88, and the lock shrinks. Five review findings were fixed on the branch, four of them by the lead after messages stopped reaching the agent: a pre-existing CFLAGS silently dropped the vendored musl headers, the header unpack could be read while being written, a two-paragraph pre-flight refusal destroyed the summary table, and the harness accepted a non-executable file as a compiler.
- 2026-09-09 01:08 UTC  DOGFOOD  A rustible workspace now exists in my_infra (untracked): playbooks/packages.rs manages apt packages on this machine and ran changed-then-ok, and playbooks/outpost/rustible_user.rs creates an account on the outpost from GitHub keys. The second is what surfaced the TLS floor.
- 2026-09-09 01:13 UTC  M7  PR #27 restored three pieces of the ring review round that were lost when the lead merged #26 and removed the worktree mid-edit: an end-to-end test that a two-paragraph failure reason cannot break the summary table (the helper alone was tested), a corrected comment in the harness which had called glibc headers "the right ones" for musl-targeted code rather than naming the measured compromise, and the answers to three questions, all measured: a pre-flight refusal exits 2, and it connects before it refuses because the target triple comes from probing the host, so a fleet gives one refusal rather than one per host and nothing is uploaded. Review finding 2 does not reproduce, with the command to show it: ring drops libc includes only for non-x86_64 musl, and every real clang install brings libc headers.

