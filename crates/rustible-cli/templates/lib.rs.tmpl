//! Code shared across this workspace's playbooks.
//!
//! Playbook files are modules of the bin crate, so they reach this library by
//! the package name, `{{crate_ident}}::helper()`, not `crate::helper()`.
